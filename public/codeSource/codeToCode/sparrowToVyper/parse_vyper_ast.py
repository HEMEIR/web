/*
 * generated by Xtext 2.28.0
 */
package org.xtext.example.mydsl.generator

import java.time.LocalDateTime
import java.time.temporal.ChronoUnit
import org.eclipse.emf.common.util.EList
import org.eclipse.emf.ecore.resource.Resource
import org.eclipse.xtext.generator.AbstractGenerator
import org.eclipse.xtext.generator.IFileSystemAccess2
import org.eclipse.xtext.generator.IGeneratorContext
import org.xtext.example.mydsl.sparrow.AdditionExpression
import org.xtext.example.mydsl.sparrow.Address
import org.xtext.example.mydsl.sparrow.AllNumber
import org.xtext.example.mydsl.sparrow.BeforePoint
import org.xtext.example.mydsl.sparrow.CompareString
import org.xtext.example.mydsl.sparrow.Condition
import org.xtext.example.mydsl.sparrow.ContractMessage
import org.xtext.example.mydsl.sparrow.Duration
import org.xtext.example.mydsl.sparrow.ExclusiveExpression
import org.xtext.example.mydsl.sparrow.FailResult
import org.xtext.example.mydsl.sparrow.FloatExpression
import org.xtext.example.mydsl.sparrow.GetPeriodExpression
import org.xtext.example.mydsl.sparrow.Group
import org.xtext.example.mydsl.sparrow.Initialize
import org.xtext.example.mydsl.sparrow.MixExpression
import org.xtext.example.mydsl.sparrow.Model
import org.xtext.example.mydsl.sparrow.Now
import org.xtext.example.mydsl.sparrow.Object
import org.xtext.example.mydsl.sparrow.ObjectExpress
import org.xtext.example.mydsl.sparrow.ObjectExpression
import org.xtext.example.mydsl.sparrow.OperateLink
import org.xtext.example.mydsl.sparrow.Operation
import org.xtext.example.mydsl.sparrow.OtherExpression
import org.xtext.example.mydsl.sparrow.OtherMixExpression
import org.xtext.example.mydsl.sparrow.ParallelExpression
import org.xtext.example.mydsl.sparrow.PeriodExpression
import org.xtext.example.mydsl.sparrow.PersonExpression
import org.xtext.example.mydsl.sparrow.RegularExpression
import org.xtext.example.mydsl.sparrow.RegularRuleExpression
import org.xtext.example.mydsl.sparrow.Require
import org.xtext.example.mydsl.sparrow.Right
import org.xtext.example.mydsl.sparrow.RuleExpression
import org.xtext.example.mydsl.sparrow.RuleStructure
import org.xtext.example.mydsl.sparrow.RuleTimeExpression
import org.xtext.example.mydsl.sparrow.StringExpression
import org.xtext.example.mydsl.sparrow.Subject
import org.xtext.example.mydsl.sparrow.SubjectExpress
import org.xtext.example.mydsl.sparrow.SubjectExpression
import org.xtext.example.mydsl.sparrow.ThingExpression
import org.xtext.example.mydsl.sparrow.ThisBoolean
import org.xtext.example.mydsl.sparrow.ThisDate
import org.xtext.example.mydsl.sparrow.ThisDecimal
import org.xtext.example.mydsl.sparrow.ThisString
import org.xtext.example.mydsl.sparrow.TimeSub
import org.xtext.example.mydsl.sparrow.Timepoint
import org.xtext.example.mydsl.sparrow.TotalCondition
import org.xtext.example.mydsl.sparrow.TotalOperation
import org.xtext.example.mydsl.sparrow.Value
import org.xtext.example.mydsl.sparrow.WithinPoint
import org.xtext.example.mydsl.sparrow.changeContract
import org.xtext.example.mydsl.sparrow.changeExpression
import org.xtext.example.mydsl.sparrow.changeOther
import org.xtext.example.mydsl.sparrow.changeAddress
import org.xtext.example.mydsl.sparrow.changeRule
import org.xtext.example.mydsl.sparrow.changeString
import org.xtext.example.mydsl.sparrow.checkExpression
import org.xtext.example.mydsl.sparrow.everyMessage
import org.xtext.example.mydsl.sparrow.isDone
import org.xtext.example.mydsl.sparrow.isTime
import org.xtext.example.mydsl.sparrow.isTrue
import org.xtext.example.mydsl.sparrow.logic
import org.xtext.example.mydsl.sparrow.messageExpression
import org.xtext.example.mydsl.sparrow.otherchange
import org.xtext.example.mydsl.sparrow.transferExpression
import org.xtext.example.mydsl.sparrow.url
import org.xtext.example.mydsl.sparrow.initExpressions
import org.xtext.example.mydsl.sparrow.initExpressiono
import org.xtext.example.mydsl.sparrow.trueOperation
import org.xtext.example.mydsl.sparrow.TrueCondition
import java.util.Map
import java.util.HashMap
import java.util.regex.Pattern
import java.util.List
import java.util.ArrayList
import static extension org.eclipse.xtext.xbase.lib.IterableExtensions.*
import static extension org.eclipse.xtext.xbase.lib.CollectionLiterals.*
import java.util.Arrays
import org.eclipse.emf.ecore.EObject

/**
 * Generates code from your model files on save.
 * 
 * See https://www.eclipse.org/Xtext/documentation/303_runtime_concepts.html#code-generation
 */
class SparrowGenerator extends AbstractGenerator {
	
	//时间限制
	var int time = 1
	//条件完成数
	var int con = 1
	//排他网关数量
	var int exclu = 1
	override void doGenerate(Resource res, IFileSystemAccess2 fsa, IGeneratorContext context) {
		for (e : res.allContents.toIterable.filter(Model)){
		
		val astNodeCount = countASTNodes(e)
			println(e.name + " AST Node Count: " + astNodeCount)
		
		val currentDateTimestart1 = LocalDateTime.now()
		fsa.generateFile(
		"sparrow2solidity/" + e.name + ".sol",
		e.compileSolidity)
		
		val currentDateTimeend1 = LocalDateTime.now()
		val duration2 = ChronoUnit.MILLIS.between(currentDateTimestart1, currentDateTimeend1)
		println(e.name+"_solidity" + ": " + duration2)
		
		val currentDateTimestart2 = LocalDateTime.now()
		fsa.generateFile(
		"sparrow2go/" + e.name + ".go",
		e.compileGo)
		
		val currentDateTimeend2 = LocalDateTime.now()
		val duration3 = ChronoUnit.MILLIS.between(currentDateTimestart2, currentDateTimeend2)
		println(e.name+"_go" + ": " + duration3)
		
		val currentDateTimestart3 = LocalDateTime.now()
		fsa.generateFile(
		"sparrow2Vyper/" + e.name + ".vy",
		e.compileVyper)
		
		val currentDateTimeend3 = LocalDateTime.now()
		val duration4 = ChronoUnit.MILLIS.between(currentDateTimestart3, currentDateTimeend3)
		println(e.name+"_vyper" + ": " + duration4)
					
		}
	}
	
//	def compileGo(Model model)'''
//	this is a go file.
//	'''

	def Integer countASTNodes(EObject element) {
	    if (element === null) return 0
	    var Integer count = 1 // 显式声明为 Integer
	    for (child : element.eContents) {
	        count = count + countASTNodes(child) // 避免 += 可能的类型推断错误
	    }
	    return count
	}

	
	def compileGo(Model model)'''
	package main
	import (
		"encoding/json"
		"fmt"
		"time"
		«FOR name : model.packageName»
		import "«name»";
		«ENDFOR»
	
		"github.com/hyperledger/fabric-contract-api-go/contractapi"
	)
	type «model.name» struct {
		contractapi.Contract
		functionStatus map[string]bool
		functionFinishTime map[string]int64
		contractState string
		contractMessage *ContractMessage
	}
«««	go里面没有继承
	«FOR subject:model.subjects»
		«subject.compileSubjectGo»
	«ENDFOR»
	
	«FOR object:model.objects»
		«object.compileObjectGo»
	«ENDFOR»
	
	«IF model.contractMessage !== null»
		«model.contractMessage.compileMessageGo»
	«ENDIF»
	func (s *«model.name») Init(ctx contractapi.TransactionContextInterface) error {
«««	初始化所有信息
	«IF model.initialize !== null»
		«model.initialize.compileInitGo»
	«ENDIF»
	
	«FOR group : model.group»
		«group.compileGroupGo»
	«ENDFOR»
	
	«IF model.contractMessage !== null»
		«model.contractMessage.compileMessageGo2»
	«ENDIF»
	
	}
	
	«IF model.operations !== null»
		«model.operations.compileOperationGo(model)»
	«ENDIF»
	«IF model.conditions !== null»
		«model.conditions.compileConditionGo(model)»
	«ENDIF»
	«IF model.ruleStructures!==null»
		«model.ruleStructures.compileRuleGo(model)»
	«ENDIF»
	
	«IF model.require !== null »
		«model.require.compileRequireGo(model)»
	«ENDIF»
	
	// changeRule 方法用于更新 functionStatus 和 functionFinishTime 的值
	func ChangeRule(expressionName string,s *«model.name») {
		// 更新 functionStatus
		s.functionStatus[expressionName] = true
		// 更新 functionFinishTime
		s.functionFinishTime[expressionName] = int64(time.Now().Unix())
	}
	
	func OnlyState(State string) bool {
		if CompareStrings(State, "start") || CompareStrings(State, "restart"){ 
			return true
		}
		return false
	}
	
«««	func onlySomeone(ctx contractapi.TransactionContextInterface, s Person) {
«««		//如何限制角色对功能的调用？
«««		//好像也是要链接外部？
«««	}
	func OnlySomeone(ctx contractapi.TransactionContextInterface, expectedPerson Person) error {
		// 获取调用者的 MSP ID
		callerMSPID, err := ctx.GetClientIdentity().GetMSPID()
		if err != nil {
			return fmt.Errorf("failed to get caller MSP ID: %v", err)
		}
	
		// 验证调用者是否符合预期的 MSP ID
		if callerMSPID != expectedPerson.MSPID {
			return fmt.Errorf("caller does not have the required permissions: expected MSPID %s, got %s", expectedPerson.MSPID, callerMSPID)
		}
	
		// 验证通过
		return nil
	}
	
	func main() {
		// 初始化Contract结构体的map变量
«««		contract := &«model.name»{
«««			functionStatus: make(map[string]bool),
«««			functionFinishTime: make(map[string]int),
«««			contractState: "start",
«««		}
		contract := new(«model.name»)
	
		// 启动Fabric链码服务
		cc, err := contractapi.NewChaincode(contract)
		if err != nil {
			fmt.Printf("Error starting SimpleContract chaincode: %v\n", err)
			return
		}
	
		if err := cc.Start(); err != nil {
			fmt.Printf("Error starting SimpleContract chaincode: %v\n", err)
		}
		
	«FOR i : model.ruleStructures.manyRuleExpression»
		«i.compileManyRuleGo»
	«ENDFOR»
	}
	'''
		
	def compileGroupGo(Group group){
		var string1=''
		var boolean first=true
		for(i:group.value){
			if(!first){
			string1+=','+i.name
			}
			else {
				string1+=i.name
				first=false
			}
			}
		string1=group.name+':= []'+group.subtype.name+'{'+string1+'}'
		return string1
	}
			
	def compileSubjectGo(Subject subject)'''
	type «subject.name» struct {
	  MSPID  string `json:"MSPID"`
	  Name   string `json:"name"`
	«IF subject.type == "CA"»
	Key  int `json:"key"`
	Year   int `json:"year"`
	«ENDIF»
	«IF subject.subjectExpression !== null»
		«subject.subjectExpression.compileSubjectExpressionGo»
	«ENDIF»
	}
	'''
	
	def compileSubjectExpressionGo(SubjectExpression SuE)'''
	«FOR key :  SuE.keyValue»
		«IF key.type == "fixed"»
		«key.name.toFirstUpper»	float `json:"«key.name»"`
		«ELSEIF key.type == "date"»
		«key.name.toFirstUpper»	string `json:"«key.name»"`
		«ELSE»
		«key.name.toFirstUpper» «key.type» `json:"«key.name»"`
		«ENDIF»
	«ENDFOR»
	'''
	
	def compileObjectGo(Object object)'''
	type «object.name» struct {
	«IF object.objectExpression !== null»
		«object.objectExpression.compileObjectExpressionGo»
	«ENDIF»
	}
	'''
	
	def compileObjectExpressionGo(ObjectExpression ObE)'''
	«FOR key :  ObE.keyValue»
		«IF key.type == "fixed"»
		«key.name.toFirstUpper»	float64 `json:"«key.name»"`
		«ELSEIF key.type == "date"»
		«key.name.toFirstUpper»	string `json:"«key.name»"`
		«ELSEIF key.type == "ufixed"»
				«key.name.toFirstUpper»	float64 `json:"«key.name»"`
		«ELSEIF key.type == "uint"»
				«key.name.toFirstUpper»	int64 `json:"«key.name»"`
		«ELSE»
		«key.name.toFirstUpper» «key.type» `json:"«key.name»"`
		«ENDIF»
	«ENDFOR»
	'''
	
	def compileMessageGo(ContractMessage message)'''
	type ContractMessage struct{
		Status string `json:"status"`
		«FOR key :  message.message»
			«IF key.type.type == "fixed"»
				«key.type.name.toFirstUpper» float64 `json:"«key.type.name»"`
			«ELSEIF key.type.type == "date"»
				«key.type.name.toFirstUpper» int64 `json:"«key.type.name»"`
			«ELSEIF key.type.type == "ufixed"»
				«key.type.name.toFirstUpper» float64 `json:"«key.type.name»"`
			«ELSEIF key.type.type == "duration"»
				«key.type.name.toFirstUpper» int64 `json:"«key.type.name»"`
			«ELSEIF key.type.type == "uint"»
				«key.type.name.toFirstUpper» int64 `json:"«key.type.name»"`
			«ELSE»
				«key.type.name.toFirstUpper» «key.type.type» `json:"«key.type.name»"`				
			«ENDIF»
		«ENDFOR»
	}
	'''
	
	
	def compileMessageGo2(ContractMessage message)'''
	// 初始化链码状态
	s.functionStatus = make(map[string]bool)
	s.functionFinishTime= make(map[string]int64)
	s.contractState = "start"
	s.contractMessage = &ContractMessage{
		Status: "start",
		«FOR key :  message.message»
			«key.type.name.toFirstUpper»: «key.value.compilevalue» ,
		«ENDFOR» 
	}
	// 存储 ContractMessage 到世界状态
	contractMessageJSON, _ := json.Marshal(s.contractMessage)
		if err := ctx.GetStub().PutState("ContractMessage", contractMessageJSON); err != nil {
			return fmt.Errorf("failed to put ContractMessage to world state: %v", err)
		}
	
		// 初始化状态和完成时间
		if err := ctx.GetStub().PutState("ContractState", []byte("start")); err != nil {
			return fmt.Errorf("failed to initialize contract state: %v", err)
		}
		return nil
	
	
	'''
	
	def compileInitGo(Initialize init)'''
	«««初始化主体
			«FOR sinit:init.inits»
				«sinit.name» := «sinit.subtype.name»{«sinit.compileSinitGo»}
				«sinit.name»JSON, _ := json.Marshal(«sinit.name»)
				if err := ctx.GetStub().PutState("«sinit.name»", «sinit.name»JSON); err != nil{
					return fmt.Errorf("failed to put «sinit.name» to world state. %v", err)
				}
				
			«ENDFOR»
	«««初始化客体
			«FOR oinit:init.inito»
				«oinit.name» := «oinit.obtype.name»{«oinit.compileOinitGo»}
				«oinit.name»JSON, _ := json.Marshal(«oinit.name»)
				if err := ctx.GetStub().PutState("«oinit.name»", «oinit.name»JSON); err != nil{
					return fmt.Errorf("failed to put «oinit.name» to world state. %v", err)
				}
				
			«ENDFOR»
	'''
	
	def compileOinitGo(initExpressiono expressiono){
		val length=expressiono.value.size
		var valueString=''
		for(var i=0; i<length;i++){
			valueString+=expressiono.obtype.objectExpression.keyValue.get(i).name.toFirstUpper
			+':'+expressiono.value.get(i).compilevalueGo+','
		}
		return valueString
	}
	
	def compileSinitGo(initExpressions expressions){
		val length=expressions.value.size
		var valueString=''
		if(expressions.subtype.type=='CA'){
			for(var i=0; i<length;i++){
				if(i==0){
					valueString+='Name:'+expressions.value.get(i).compilevalueGo+','
				}
				if(i==1){
					valueString+='MSPID:'+expressions.value.get(i).compilevalueGo+','
				}
				if(i==2){
					valueString+='Year:'+expressions.value.get(i).compilevalueGo+','
				}
				if(i==3){
					valueString+='Key:'+expressions.value.get(i).compilevalueGo+','
				}
				else{
					if(length>4)
					valueString+=expressions.subtype.subjectExpression.keyValue.get(i-4).name.toFirstUpper
					+':'+expressions.value.get(i).compilevalueGo+','
				}
			}
		}
		else{
			for(var i=0; i<length;i++){
				if(i==0){
					valueString+='Name:'+expressions.value.get(i).compilevalueGo+','
				}
				if(i==1){
					valueString+='MSPID:'+expressions.value.get(i).compilevalueGo+','
				}
				else{
//					println(expressions.name+expressions.subtype.name+'length'+length)
					if(length>2)
					valueString+=expressions.subtype.subjectExpression.keyValue.get(i-2).name.toFirstUpper
					+':'+expressions.value.get(i).compilevalueGo+','
				}
			}
		}
		return valueString
	}
	
	def dispatch compilevalueGo(Address value){
		return value.value
	}
	
	def dispatch compilevalueGo(AllNumber value){
		return value.number
	}
	
	def dispatch compilevalueGo(Now value){
		return 'time.Now()'
	}
	
	def dispatch compilevalueGo(ThisDecimal value){
		return stringToDouble(value.value)*1000
	}
	
	def dispatch compilevalueGo(url value){
		return value.value
	}
	
	def dispatch compilevalueGo(ThisString value){
		return '"'+value.value+'"'
	}
	
	def dispatch compilevalueGo(ThisBoolean value){
		return value.value
	}
	
	def dispatch compilevalueGo(Duration value){
		if(value.symbol=="months")
			return (value.value)*60*60*24*30
		else if(value.symbol=="days")
			return (value.value)*60*60*24
		else if(value.symbol=="mins")
			return (value.value)*60
		else if(value.symbol=="hours")
			return (value.value)*60*60
		else if(value.symbol=="years")
			return (value.value)*60*60*24*30*365
	}
	
	def dispatch compilevalueGo(ThisDate value){
		return toTimestamp(value.value.year,value.value.month,value.value.day,value.value.hour,value.value.min)
	}
	
	def dispatch compilevalueGo(Right value){
		return '"'+value.right.name+'"'
	}
	
	
	
	
	
	def compileConditionGo(Condition condition,Model model){
		var myString = ''''''
	    for (everycondition : condition.conditions) {
	        var string1 = '''func (s *«model.name») «everycondition.name.toFirstUpper» (ctx contractapi.TransactionContextInterface) bool {'''
	        var string2=''
	        
			for (everylink : everycondition.andOrLink) {
	            if (everylink.link == 'and') {
	            	if(everylink.condition!==null){
		                string2 += everylink.condition.condition.compileTrueConditionGo3(model)
		            }
	                
	            } else if (everylink.link == 'or') {
		                string2 += everylink.condition.condition.compileTrueConditionGo3(model)
	                }
	                
	        }
	        
	        if(everycondition.conditionExpression!==null)
	        	if(everycondition.conditionExpression.no!==null)
	        		string2 ='!'+everycondition.conditionExpression.condition.compileTrueConditionGo(model)
	        	else
	        		string2 = everycondition.conditionExpression.condition.compileTrueConditionGo(model)
	        else if(everycondition.linkCondition!==null)
	        	if(everycondition.linkCondition.no!==null)
	        		string2 ='if (' +'!'+'s.'+everycondition.linkCondition.linkCondition.name.toFirstUpper+'(ctx)'
	        	else
	        		string2 ='if (' + 's.'+ everycondition.linkCondition.linkCondition.name.toFirstUpper+'(ctx)'
	        for (everylink : everycondition.andOrLink) {
	            if (everylink.link == 'and') {
	            	if(everylink.condition!==null){
		            	if(everylink.condition.no!==null)
		                string2 += '&&' +" !"+everylink.condition.condition.compileTrueConditionGo2(model)
		                else
		                string2 += '&&' +everylink.condition.condition.compileTrueConditionGo2(model)
	                }
	                if(everylink.linkCondition!==null){
		            	if(everylink.linkCondition.no!==null)
		                string2 += '&&' +" !"+'s.'+everylink.linkCondition.linkCondition.name.toFirstUpper+'(ctx)'
		                else
		                string2 += '&&' +'s.'+ everylink.linkCondition.linkCondition.name.toFirstUpper+'(ctx)'
	                }
	            } else if (everylink.link == 'or') {
	            	if(everylink.condition.no!==null)
		                string2 += '||' +" !"+everylink.condition.condition.compileTrueConditionGo2(model)
		                else
		                string2 += '||' +everylink.condition.condition.compileTrueConditionGo2(model)
	                }
	                if(everylink.linkCondition!==null){
		            	if(everylink.linkCondition.no!==null)
		                string2 += '||' +" !"+'s.'+everylink.linkCondition.linkCondition.name.toFirstUpper+'(ctx)'
		                else
		                string2 += '||' +'s.'+ everylink.linkCondition.linkCondition.name.toFirstUpper+'(ctx)'
	            }
	        }
	        string2 += '){'
	        myString +='''
	    «string1»
	    	«string2» 
	    		return true
	    	}
	    	return false
	    }
	    '''
	    }
	    return myString
	}
	
	
//	def dispatch compileTrueConditionGo(Timepoint timepoint){
//		return timepoint.compileTimePoint2Go
//	}
	
	def dispatch compileTimePoint2Go(WithinPoint timepoint){
		var string='''
			«IF timepoint.time=='months'»
				!IsTime(s.functionFinishTime["«timepoint.thing.name»"]+60*60*24*30*«timepoint.number»)
			«ELSEIF timepoint.time=='days'»
				!IsTime(s.functionFinishTime["«timepoint.thing.name»"]+60*60*24*«timepoint.number»)
			«ELSEIF timepoint.time=='hours'»
				!IsTime(s.functionFinsihTime["«timepoint.thing.name»"]+60*60*«timepoint.number»)
			«ENDIF»
			'''
		return string
	}
	
	def dispatch compileTimePoint2Go(BeforePoint timepoint){
		if(timepoint.lineTime!==null)
//		datetime.toTimestamp("+condition.date.value.compilevalue+
			return "!IsTime(datetime.toTimestamp("+timepoint.lineTime.compilevalue+"))"
		else if(timepoint.value!==null)
			return "!IsTime("+"s.contractMessage."+timepoint.value.name.capitalize+")"
	}
	
	def dispatch compileTrueConditionGo(Timepoint timepoint,Model model){
		return 'if ('+ timepoint.compileTimePoint2Go
	}
	
	
	def dispatch compileTrueConditionGo(TimeSub condition,Model model){
		var String a=condition.valueA.compileExpression
		var String b=''
		if(condition.valueB!==null)
			b=condition.valueB.compileExpression
		if(condition.valueC!==null)
			b='s.functionFinishTime[\"'+condition.valueC.name+'\"]'
		var c=condition.duration.compilevalue
		return 'if (CompareTimestamps('+a+','+b+','+c+',\"'+condition.compare+'\")'
	}
	
	def dispatch compileTrueConditionGo(CompareString condition, Model model){
		
		return 'if (CompareStrings('+condition.valueA.compileExpressionGo+','+condition.valueB.compileExpressionGo+')'
	}
	
	def dispatch compileTrueConditionGo(checkExpression condition,Model model){
		return "if (Check("+condition.name.name+")"
	}
	def dispatch compileTrueConditionGo(isTrue condition,Model model){
		return 'if ('+condition.compare.compileSingleExpressionGo
	}
	
	def dispatch compileTrueConditionGo(isDone condition,Model model){
		return 'if (IsDone(ctx,\"'+condition.name.name+'\",s)'
	}
	
	
	//改到这里了
	def dispatch compileTrueConditionGo(logic condition,Model model){

		val valueA = replaceVariables(condition.valueA.compileExpression, model)
        val valueB = replaceVariables(condition.valueB.compileExpression, model)
        // 检查是否需要插入 token 获取逻辑
        val tokenRequired = containsTokenVariable(valueA) || containsTokenVariable(valueB)
        
        if (tokenRequired) {
            '''
            tokenBytes, err := ctx.GetStub().GetState("Service")
            if err != nil {
            	return false // 出现错误时返回 false
            }
            if tokenBytes == nil {
            	return false // 如果找不到 token 数据，返回 false
            }
            
            // 将获取到的 token 数据反序列化为 token 结构体
            var token token
            if err := json.Unmarshal(tokenBytes, &token); err != nil {
                return false // 反序列化失败时返回 false
            }
            if (Logic('''+valueA+','+valueB+',\"'+condition.mathSymbol+'\")'
            
        }		
	}
	
	def dispatch compileTrueConditionGo2(isTime condition,Model model){
		var string=''
		if(condition.expression !== null)
			if(condition.duration===null){
				val valueExpr = processExpressionInt64(condition.expression.compileExpression, model)
				string='IsTime('+valueExpr+')'
			}
				
			else if(condition.duration!==null)
				string='IsTime('+'s.contractMessage.'+condition.expression.compileExpression.capitalize+condition.symbol+condition.duration.compilevalue+')'
			if(condition.value !== null)
				if(condition.duration===null)
					string='IsTime(s.functionFinishTime[\"'+condition.value.name+'\"]){'
				else if(condition.duration!==null)
					string='IsTime(s.functionFinishTime[\"'+condition.value.name+'\"]+'+condition.symbol+condition.duration.compilevalue+'){'
		return string
	}
	
	def dispatch compileTrueConditionGo2(Timepoint timepoint,Model model){
		return timepoint.compileTimePoint2Go
	}
	
	def dispatch compileTrueConditionGo2(TimeSub condition,Model model){
		var String a=condition.valueA.compileExpressionGo
		var String b=''
		if(condition.valueB!==null)
			b=condition.valueB.compileExpressionGo
		if(condition.valueC!==null)
			b='s.functionFinishTime[\"'+condition.valueC.name+'\"]'
		var c=condition.duration.compilevalueGo
		return 'CompareTimestamps('+a+','+b+','+c+',\"'+condition.compare+'\")'
	}
	
	def dispatch compileTrueConditionGo2(CompareString condition, Model model){
		return 'CompareStrings('+condition.valueA.compileExpressionGo+','+condition.valueB.compileExpressionGo+')'
	}
	
	def dispatch compileTrueConditionGo2(checkExpression condition,Model model){
		return "Check("+condition.name.name+")"
	}
	def dispatch compileTrueConditionGo2(isTrue condition,Model model){
		return condition.compare.compileSingleExpressionGo
	}
	
	def dispatch compileTrueConditionGo2(isDone condition,Model model){
		return 'IsDone(ctx,\"'+condition.name.name+'\",s)'
	}
	
	

	def dispatch compileTrueConditionGo2(logic condition,Model model){

		val valueA = replaceVariables(condition.valueA.compileExpressionGo, model)
        val valueB = replaceVariables(condition.valueB.compileExpressionGo, model)
        // 检查是否需要插入 token 获取逻辑
        val tokenRequired = containsTokenVariable(valueA) || containsTokenVariable(valueB)
        var string=''

        string +=  "Logic("+valueA+','+valueB+',\"'+condition.mathSymbol+'\")'
      
       
	    return string	
	}
	
	def compileExpressionGo(MixExpression expression){
		var string=''
		string+=expression.expression.compileSingleExpressionGo
		if(expression.otherMixExpression!==null)
			string+=expression.otherMixExpression.compileOtherMixExpressionGo
		return string
	}
	
	def compileExpressionGo2(MixExpression expression){
		var string=''
		string+=expression.expression.compileSingleExpressionGo2
		if(expression.otherMixExpression!==null)
			string+=expression.otherMixExpression.compileOtherMixExpressionGo2
		return string
	}
	
	def dispatch compileTrueConditionGo3(isTime condition,Model model){
		return " "
	}
	
	def dispatch compileTrueConditionGo3(Timepoint timepoint,Model model){
		return " "
	}
	
	def dispatch compileTrueConditionGo3(TimeSub condition,Model model){
		return " "
	}
	
	def dispatch compileTrueConditionGo3(CompareString condition, Model model){
		return " "
	}
	
	def dispatch compileTrueConditionGo3(checkExpression condition,Model model){
		return " "
	}
	def dispatch compileTrueConditionGo3(isTrue condition,Model model){
		return " "
	}
	
	def dispatch compileTrueConditionGo3(isDone condition,Model model){
		return " "
	}
	
	

	def dispatch compileTrueConditionGo3(logic condition,Model model){

		val valueA = replaceVariables(condition.valueA.compileExpressionGo, model)
        val valueB = replaceVariables(condition.valueB.compileExpressionGo, model)
        // 检查是否需要插入 token 获取逻辑
        val tokenRequired = containsTokenVariable(valueA) || containsTokenVariable(valueB)
        //var string=''
        var string = new StringBuilder
        if (tokenRequired) {
            string.append('''
            tokenBytes, _ := ctx.GetStub().GetState("Service")
           
            var token token
            json.Unmarshal(tokenBytes, &token)''')
           
			//return "tokenRequired"
            
        }
        //string +=  "logic("+valueA+','+valueB+',\"'+condition.mathSymbol+'\")'
        //   else return "0"
      //  var string = new StringBuilder
        val regexPrefix = '^([a-zA-Z]+)\\.'
	    val pattern = Pattern.compile(regexPrefix)

		
		 
   		 val alreadyDeserialized = newHashSet // Xtend 的 HashSet 初始化方式

		
		// 判断是否需要反序列化 valueA
		   if (!(valueA.startsWith("time.Now()") || valueA.startsWith("s.contractMessage"))) {
	        val matcherA = pattern.matcher(valueA)
	        if (matcherA.find) {
	            val groupA = matcherA.group(1) // 提取 "Buyer" 或类似的前缀
	            if (!alreadyDeserialized.contains(groupA)) { // 如果尚未反序列化
	                string.append('''
	                    ''' + groupA + '''Bytes, _ := ctx.GetStub().GetState("''' + groupA + '''")
	                    var ''' + groupA + ''' Person
	                    json.Unmarshal(''' + groupA + '''Bytes, &''' + groupA + ''')
	                ''')
	                alreadyDeserialized.add(groupA) // 标记为已反序列化
	            }
	        }
	    }
	    // 判断是否需要反序列化 valueB
	    if (!(valueB.startsWith("time.Now()") || valueB.startsWith("s.contractMessage"))) {
	        val matcherB = pattern.matcher(valueB)
	        if (matcherB.find) {
	            val groupB = matcherB.group(1) // 提取 "Buyer" 或类似的前缀
	            if (!alreadyDeserialized.contains(groupB)) { // 如果尚未反序列化
	                string.append('''
	                    ''' + groupB + '''Bytes, _ := ctx.GetStub().GetState("''' + groupB + '''")
	                    var ''' + groupB + ''' Person
	                    json.Unmarshal(''' + groupB + '''Bytes, &''' + groupB + ''')
	                ''')
	                alreadyDeserialized.add(groupB) // 标记为已反序列化
	            }
	        }
    }
	    return string.toString	
	}
	
	
	
	
	def boolean containsTokenVariable(String expression) {
        return expression.contains("token.")
    }
	

	def List<String> extractVariables(String expression) {
	    println("Extracting variables from expression: " + expression)
	    
	    val regex = "[a-zA-Z_][a-zA-Z0-9_]*" // 使用双引号
	    val pattern = Pattern.compile(regex)
	    val matcher = pattern.matcher(expression)
	
	    val vars = new ArrayList<String>() // 使用 Java 的 ArrayList
	
	    while (matcher.find()) {
	        val varName = matcher.group(0)
	        println("Found variable: " + varName)
	        vars.add(varName)
	    }
	    return vars
	}
	
	
	 def String replaceVariables(String expression, Model model) {
			println("Original expression: " + expression)
		    val varMap = createVariableMap(model)
		    println("Variable map: " + varMap)
		    val result = new StringBuilder()
		    var lastEnd = 0
			
			// 匹配以 "s.contractMessage." 开头的表达式
		    val regexContractMessage = '^(s\\.contractMessage\\.)(\\w+)' // 匹配 s.contractMessage.<字段>
		    val patternContractMessage = Pattern.compile(regexContractMessage)
		    val matcherContractMessage = patternContractMessage.matcher(expression)
			if (matcherContractMessage.find()) {
		        val prefix = matcherContractMessage.group(1) // "s.contractMessage."
		        val field = matcherContractMessage.group(2) // 第一个字段名
		        val capitalizedField = capitalize(field) // 将字段名首字母大写
		        result.append(prefix).append(capitalizedField)
		
		        // 如果表达式还有剩余部分
		        if (matcherContractMessage.end() < expression.length()) {
		            result.append(expression.substring(matcherContractMessage.end()))
		        }
	    	} else {
	        // 匹配以 "xxx." 开头的其他表达式
		        val regexPrefix = '^([a-zA-Z]+\\.)(\\w+)' // 匹配一般的 xxx.<字段>W
		        val patternPrefix = Pattern.compile(regexPrefix)
		        val matcherPrefix = patternPrefix.matcher(expression)
		
		        if (matcherPrefix.find()) {
		            val prefix = matcherPrefix.group(1) // "xxx."
		            val wordToCapitalize = matcherPrefix.group(2) // 第一个单词
		            val capitalizedWord = capitalize(wordToCapitalize)
		            result.append(prefix).append(capitalizedWord)
		
		            // 如果表达式还有剩余部分
		            if (matcherPrefix.end() < expression.length()) {
		                result.append(expression.substring(matcherPrefix.end()))
		            }
		        } else {
	            // 如果不以 "xxx." 开头，则匹配 varMap
		            val regex = '\\b(' + varMap.keySet().join('|') + ')\\b'
		            val pattern = Pattern.compile(regex)
		            val matcher = pattern.matcher(expression)
		
		            while (matcher.find()) {
		                result.append(expression.substring(lastEnd, matcher.start()))
		                val varName = matcher.group(1)
		                val mappedVar = varMap.get(varName)
		                if (mappedVar != null) {
		                    result.append(mappedVar)
		                } else {
		                    result.append(varName)
		                }
		                lastEnd = matcher.end()
		            }
		            result.append(expression.substring(lastEnd))
		        }
	    		}
		
		    val finalExpression = result.toString()
		    println("Replaced expression: " + finalExpression)
		    return finalExpression
    }
	
	def Map<String, String> createVariableMap(Model model) {
        val varMap = new HashMap<String, String>()

        // 处理 token 对象的属性
        val tokenObject = model.objects.findFirst[ name == "token" ]
        if (tokenObject !== null) {
            for (keyValue : tokenObject.objectExpression.keyValue) {
                varMap.put(keyValue.name, "token." + capitalize(keyValue.name))
            }
        }

        // 处理 contractMessage 对象的属性
        val contractMessage = model.contractMessage
        if (contractMessage !== null) {
            for (messageKey : contractMessage.message) {
                varMap.put(messageKey.type.name, "s.contractMessage." + capitalize(messageKey.type.name))
            }
        }

        return varMap
    }
	
	def String capitalize(String s) {
        if (s.isEmpty) return s
        return s.substring(0,1).toUpperCase + s.substring(1)
    }
	
	
	
	def dispatch compileTrueConditionGo(isTime condition,Model model){
		var string=''
		if(condition.expression !== null)
			if(condition.duration===null)
				string='IsTime('+'s.contractMessage.'+condition.expression.compileExpression+')'
			else if(condition.duration!==null)
				string='IsTime('+'s.contractMessage.'+condition.expression.compileExpression+condition.symbol+condition.duration.compilevalue+')'
			if(condition.value !== null)
				if(condition.duration===null)
					string='IsTime(s.functionFinishTime[\"'+condition.value.name+'\"]){'
				else if(condition.duration!==null)
					string='IsTime(s.functionFinishTime[\"'+condition.value.name+'\"]+'+condition.symbol+condition.duration.compilevalue+'){'
		return string
	}
	
	
	def dispatch compileSingleExpressionGo(RegularExpression expression){
		return expression.compileRegularGo
	}
	
	def dispatch compileRegularGo(AllNumber value){
		return value.number
	}
	
	def dispatch compileRegularGo(ThisDecimal value){
		return stringToDouble(value.value)*1000
	}
	
	def dispatch compileRegularGo(ThisBoolean value){
		return value.value
	}
	
	def dispatch compileRegularGo(Now value){
		return 'time.Now().Unix()'
	}
	
	def dispatch compileRegularGo(ThisDate value){
		return value.compilevalue
	}
	
	def dispatch compileSingleExpressionGo(OtherExpression expression){
		return '''s.contractMessage.'''+expression.value.toFirstUpper
		
	}
	
	def dispatch compileSingleExpressionGo(FloatExpression expression){
		return expression.value+'/1000'
	}
	
	def dispatch compileSingleExpressionGo(PeriodExpression expression){
		if(expression.type=="hours")
			return expression.value*60*60
		else if(expression.type=="days")
			return expression.value*60*60*24
		else if(expression.type=="months")
			return expression.value*60*60*24*30
		else if(expression.type=="years")
			return expression.value*60*60*24*30*365
		else if(expression.type=="mins")
			return expression.value*60
	}
	
	def dispatch compileSingleExpressionGo(GetPeriodExpression expression){
		if(expression.type=="hours")
			return expression.value+'*60*60'
		else if(expression.type=="days")
			return expression.value+'*60*60*24'
		else if(expression.type=="months")
			return expression.value+'*60*60*24*30'
		else if(expression.type=="years")
			return expression.value+'*60*60*24*30*365'
		else if(expression.type=="mins")
			return expression.value+'*60'
	}
	
	def dispatch compileSingleExpressionGo(RuleTimeExpression expression){
		return 's.functionFinishTime[\"'+expression.value.name+'\"]'
	}
	
	def dispatch compileSingleExpressionGo(PersonExpression expression){
		return '\"'+expression.value.name+'\"'
	}
	
	def dispatch compileSingleExpressionGo(ThingExpression expression){
		return expression.compileThingExpressionGo
	}
	
	def dispatch compileSingleExpressionGo(StringExpression expression){
		return '\"'+expression.value+'\"'
	}
	
	
	def compileOtherMixExpressionGo(EList<OtherMixExpression> expression){
		var string=''
		for(i : expression)
			string+=i.link+i.expression.compileSingleExpressionGo
		return string
	}
	
	def compileOtherMixExpressionGo2(EList<OtherMixExpression> expression){
		var string=''
		for(i : expression)
			string+=i.link+i.expression.compileSingleExpressionGo2
		return string
	}
	
	def dispatch compileThingExpressionGo(SubjectExpress expression){
		var string=''
		string=expression.subject.name+'.'+expression.attribute
		return string
	}
	
	def dispatch compileThingExpressionGo(ObjectExpress expression){
		var string=''
		string=expression.object.name+'.'+expression.attribute
		return string	
	}
	
	
	
	def dispatch compileSingleExpressionGo2(OtherExpression expression){
		return expression.value
	}
	
	def dispatch compileSingleExpressionGo2(FloatExpression expression){
		return expression.value+'/1000'
	}
	
	def dispatch compileSingleExpressionGo2(PeriodExpression expression){
		if(expression.type=="hours")
			return expression.value*60*60
		else if(expression.type=="days")
			return expression.value*60*60*24
		else if(expression.type=="months")
			return expression.value*60*60*24*30
		else if(expression.type=="years")
			return expression.value*60*60*24*30*365
		else if(expression.type=="mins")
			return expression.value*60
	}
	
	def dispatch compileSingleExpressionGo2(GetPeriodExpression expression){
		if(expression.type=="hours")
			return expression.value+'*60*60'
		else if(expression.type=="days")
			return expression.value+'*60*60*24'
		else if(expression.type=="months")
			return expression.value+'*60*60*24*30'
		else if(expression.type=="years")
			return expression.value+'*60*60*24*30*365'
		else if(expression.type=="mins")
			return expression.value+'*60'
	}
	
	def dispatch compileSingleExpressionGo2(RuleTimeExpression expression){
		return 'functionFinishTime[\"'+expression.value.name+'\"]'
	}
	
	def dispatch compileSingleExpressionGo2(PersonExpression expression){
		return '\"'+expression.value.name+'\"'
	}
	
	def dispatch compileSingleExpressionGo2(ThingExpression expression){
		return expression.compileThingExpression
	}
	
	def dispatch compileSingleExpressionGo2(StringExpression expression){
		return '\"'+expression.value+'\"'
	}
	
	
	def dispatch compileSingleExpressionGo2(RegularExpression expression){
		return expression.compileRegularGo2
	}
	
	def dispatch compileRegularGo2(AllNumber value){
		return value.number
	}
	
	def dispatch compileRegularGo2(ThisDecimal value){
		return stringToDouble(value.value)*1000
	}
	
	def dispatch compileRegularGo2(ThisBoolean value){
		return value.value
	}
	
	def dispatch compileRegularGo2(Now value){
		return 'time.Now().Unix()'
	}
	
	def dispatch compileRegularGo2(ThisDate value){
		return value.compilevalueGo
	}
	
	

	def compileOperationGo(Operation operation, Model model) '''
	    «FOR everyoperation : operation.operates»
	    // Function: «everyoperation.name»
	    func (s *«model.name») «everyoperation.name.toFirstUpper»(ctx contractapi.TransactionContextInterface) error {
	        «IF everyoperation.firstOperation !== null»
	        «everyoperation.firstOperation.compileTrueOperationGo(model)»
	        «ENDIF»
	    }
	    «ENDFOR»
	'''
	
	def dispatch compileTrueOperationGo(trueOperation op,Model model) {
	    // 这个方法留空，Xtend 会自动根据子类型进行方法调度
	}
	

	def dispatch compileTrueOperationGo(changeContract cc,Model model) '''
	    // Update contract state
	    if err := ctx.GetStub().PutState("ContractState", []byte("«cc.changeResult»")); err != nil {
	        return fmt.Errorf("failed to update contract state to «cc.changeResult»: %v", err)
	    }
	    return nil
	'''
	
	def dispatch compileTrueOperationGo(changeString cs,Model model) '''
	    // Handle changeString operation
	    if err := ctx.GetStub().PutState("«cs.changeThing»", []byte("«cs.changeResult»")); err != nil {
	        return fmt.Errorf("failed to update «cs.changeThing» to «cs.changeResult»: %v", err)
	    }
	    return nil
	'''
	
	def dispatch compileTrueOperationGo(otherchange cs,Model model){ 
//	'''
//	    // Handle changeString operation
//	    «cs.changeThing.compileExpression»= «cs.changeResult.compileExpression»
//	    return nil
//	    	
//	'''
		var string=''
		
		var changething = cs.changeThing.compileExpression
		var foundContractMessage = false
		
		for (key : model.contractMessage.message) {
            if (key.type.name == changething) {
                foundContractMessage = true
                changething = "s.contractMessage."+changething.toFirstUpper
            }
	      }
	      
        var foundToken = 0
        if (changething.startsWith("Service.")){
        	foundToken+=1
			val suffix = changething.substring(8) // 从第 8 个字符开始截取后续部分
		    // 将第一个字母大写
		    changething = "Service." + suffix.toFirstUpper
		}
        if (!foundContractMessage) {
        	for(object:model.objects){
        		for (key : object.objectExpression.keyValue) {
        			var modifiedKeyName = key.name 
	                if (key.name == changething) {
	                	changething = "Service."+changething.toFirstUpper
	                    foundToken += 1
	                }
            	}
        	}   
        }
        	   
		val variables = parseExpression(cs.changeResult.compileExpression)
		
		// 遍历每个变量，生成相应的反序列化代码
	    for (variable : variables) {
	        // 如果是 Token 类型的变量，进行反序列化
        	for(object:model.objects){
        		for (key : object.objectExpression.keyValue) {
        			var modifiedKeyName = key.name 
	                if (key.name == variable) {
	                    foundToken+=1
	                }
            	}
        	}   
	       
	    }
	    
	    if (foundToken>=1) {
	        	string += 'ServiceBytes, _ := ctx.GetStub().GetState("Service")' + "\n"
	            string += "// 将获取到的 token 数据反序列化为 token 结构体\n"
	            string += 'var Service token\n'
	            string += "json.Unmarshal(ServiceBytes, &Service)\n"
	        }
		
// transferTo 调用
		if(cs.changeResult.compileExpressionGo2=="time.Now()"){
			val valueExpr = "int64(time.Now().Unix())"
			string+=changething+'='+valueExpr
		}else{
			
			val valueExpr = processExpression(cs.changeResult.compileExpressionGo2, model)
			
			val regexPrefix = '^([a-zA-Z]+)\\.'
	   		val pattern = Pattern.compile(regexPrefix)
			if (!(valueExpr.startsWith("time.Now()") || valueExpr.startsWith("s.contractMessage"))) {
		        val matcherA = pattern.matcher(valueExpr)
		        if (matcherA.find) {
		            val groupA = matcherA.group(1) // 提取 "Buyer" 或类似的前缀
		            if (groupA!="Service") { // 如果尚未反序列化
		                string+='''
		                    ''' + groupA + '''Bytes, _ := ctx.GetStub().GetState("''' + groupA + '''")
		                    var ''' + groupA + ''' Person
		                    json.Unmarshal(''' + groupA + '''Bytes, &''' + groupA + ''')
		                '''
		                
		            }
		        }
	    }
			
			
	    	string+=changething+'='+valueExpr+'\n'
	    	string+='return nil'
		}
		
//	    result += 'transferTo(ctx, ' + operation.person.name + ', ' + operation.value.compileExpression + ');\n'
//	    
//	    return result
		
		
		
		
		return string
	}
	
	def dispatch compileTrueOperationGo(transferExpression cs,Model model){
		var result = ""
    
    	// 反序列化 Seller
    	result += cs.person.name + '''Bytes, _ := ctx.GetStub().GetState("''' + cs.person.name + '''")
	            // 将获取到的 token 数据反序列化为 token 结构体
	            var ''' + cs.person.name + ''' Person
	            json.Unmarshal(''' + cs.person.name + '''Bytes, &''' + cs.person.name + ''')
	        '''
		
//		if(operation.value.compileExpression.startsWith("Service.")){
//			 result += 'ServiceBytes, _ := ctx.GetStub().GetState("Service")' + '''
//	            // 将获取到的 token 数据反序列化为 token 结构体
//	            var Service token
//	            json.Unmarshal(ServiceBytes, &Service)
//	        '''
//	            
//	            // 将 Service.price 转换为 Service.Price
//	           
//	            result +='transferTo(ctx,'+operation.person.name+','+"Service." + operation.value.compileExpression.substring(8).toFirstUpper+')'+';'
//    			return result
//			
//		}
//		
		val variables = parseExpression(cs.value.compileExpression)
		
		var foundToken = 0
		// 遍历每个变量，生成相应的反序列化代码
	    for (variable : variables) {
	    	var modifiedVariable = variable 
	        
	        // 如果是 ContractMessage 中的字段，前缀加 s.contractMessage
	        var foundContractMessage = false
	        for (key : model.contractMessage.message) {
	            if (key.type.name == variable) {
	                foundContractMessage = true
	            }
	        }

	        // 如果是 Token 类型的变量，进行反序列化
	        
	        if (!foundContractMessage) {
	        	for(object:model.objects){
	        		for (key : object.objectExpression.keyValue) {
	        			var modifiedKeyName = key.name 
		                if (key.name == variable) {
		                    foundToken += 1
		                }
	            	}
	        	}   
	        }
	       
	        
	        // 如果是未知变量，则直接使用原变量名
//	        if (!foundToken && !foundContractMessage) {
//            	result += modifiedVariable + "\n"
//        	}
	    }
		 if (foundToken>=1) {
	        	result += 'ServiceBytes, _ := ctx.GetStub().GetState("Service")' + "\n"
	            result += "// 将获取到的 token 数据反序列化为 token 结构体\n"
	            result += 'var Service token\n'
	            result += "json.Unmarshal(ServiceBytes, &Service)\n"
	        }
	    // transferTo 调用
//	    string += 'transferTo(ctx,' + operation.person.name + ',' + valueExpr + ')'+ ';'
//	    
//	    return string
// transferTo 调用
		
		val valueExpr = processExpressionFloat64(cs.value.compileExpression, model)
    	result += 'transferTo(ctx, ' + cs.person.name + ', ' + valueExpr + ');\n'
    	result += 'return nil'
    
    	return result
//	    result += 'transferTo(ctx, ' + operation.person.name + ', ' + operation.value.compileExpression + ');\n'
//	    
//	    return result
		//var string=''
		//string='transferTo(ctx,'+cs.person.name+',10**14*('+cs.value.compileExpression+'))'+'\n'+'return nil'
	//	return string
	}
	
	
	def compileRuleGo(RuleStructure structure,Model model)'''
		«FOR i : structure.manyRuleExpression»
			«i.compileManyRuleExpressionGo(model)»
		«ENDFOR»
	'''
	
	def dispatch compileManyRuleExpressionGo(ParallelExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileRuleExpressionGo(model)»
		«ENDFOR»
	'''
	def dispatch compileManyRuleExpressionGo(RegularRuleExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileRuleExpressionGo(model)»
		«ENDFOR»
	'''
	
	def compileRuleExpressionGo(RuleExpression expression,Model model){
		var String code=''''''
		var string1=''
		var string1_1=''
		var link=''
		var setDateString1=''
		var setDateString2=''
		if(expression.setdate!==null){
			setDateString1='_year uint16, _months uint8, _days uint8, _hours uint8, _minutes uint8'
			setDateString2=expression.setdate.message+'=convertToTimestamp(_year,_months,_days,_hours,_minutes);'
		}
		if(expression.setdate!==null&&expression.set!==null){
			link=','
		}
		if(expression.repeat===null){
		if( expression.set===null){
			if(setDateString1 == '')
				string1 = '''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+"ctx contractapi.TransactionContextInterface) {"
			else string1 = '''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+",ctx contractapi.TransactionContextInterface) {"
			//string1="func "+expression.name+"("+setDateString1+") {"
			if(expression.totalOperation.person!==null)
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"		
		}
		if(expression.set!==null){
			string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+link+expression.set.compileMessageOperateGo+",ctx contractapi.TransactionContextInterface) {"
			if(expression.totalOperation.person!==null)
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
		}
		}
		if(expression.repeat!==null){
		if(expression.set===null){
			if(setDateString1 == '') 
				string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+"ctx contractapi.TransactionContextInterface) {"
			else string1 = '''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+",ctx contractapi.TransactionContextInterface) {"
			if(expression.totalOperation.person!==null)
				string1_1="if OnlyState(s.contractState){"
			else if(expression.totalOperation.person2!==null)
				string1_1="if OnlyState(s.contractState){"
			else
				string1_1="if OnlyState(s.contractState){"		
		}
		if(expression.set!==null){
			string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+link+expression.set.compileMessageOperateGo+",ctx contractapi.TransactionContextInterface) {"
			if(expression.totalOperation.person!==null)
				string1_1="if OnlyState(s.contractState){"
			else if(expression.totalOperation.person2!==null)
				string1_1="if OnlyState(s.contractState){"
			else
				string1_1="if OnlyState(s.contractState){"
		}
		}	
		var string2=''''''
		if(expression.set!==null)
			string2+=expression.set.compileMessageOperateThingGo
		var string3=''
		if(expression.totalCondition!==null){
			if(expression.totalCondition.condition!==null){
				string3 += expression.totalCondition.condition.condition.compileTrueConditionGo3(model)	
			}
			if(expression.totalCondition.andorcondition!==null){
				for(i: expression.totalCondition.andorcondition){
					if(i.link=="and")
						if(i.condition!==null)
					 		string3 += i.condition.condition.compileTrueConditionGo3(model)
					else if(i.link=="or")
							string3 += i.condition.condition.compileTrueConditionGo3(model)
				}
			
			}
			string3=string3+'if('+expression.totalCondition.compileTotalConditionGo(model)+'){'
		}			
			
		var string4=''''''	
		string4+=expression.totalOperation.compileTotalOperationGo(model)
		
		var string5=''''''
		if(expression.subExpression!==null){
			for(i: expression.subExpression){
				var string6=''
				var string7=''''''
				var string8=''
				var string9=''
				if(i.totalExpression.totalCondition!==null)
					string6+='if('+i.totalExpression.totalCondition.compileTotalConditionGo(model)+'){'
				string7+=i.totalExpression.totalOperation.compileTotalOperationGo(model)
				if(i.totalExpression.elseExpression!==null)
					string8='''else{ 
						«i.totalExpression.elseExpression.totalOperation.compileTotalOperationGo(model)»
						}'''
				if(i.totalExpression.totalCondition!==null)
					string9="}"
				string5+='''
				 	«string6»
				 		«string7»
				 	«string8»
				 	«string9»
				'''
				}
			}
		
		var string6="ChangeRule(\""+expression.name+"\",s);"
		var string7=''''''
		if(expression.totalCondition!==null)
			string7="}"	
		
		code+='''
		«string1»
			«string1_1»
			«setDateString2»
			«string2»
			«string3»
				«string4»
				«string5»
				«string6»
			«string7»
		}
		}
		'''
		
		if(expression.elseExpression===null){
			return code
		}
	
		if(expression.elseExpression!==null){
			var string8=''''''
			var string9=''
			var string9_1=''
		if(expression.elseExpression.set===null){
			string9='''func else (s *«model.name»)'''+expression.name.toFirstUpper+"(ctx contractapi.TransactionContextInterface){"
			if(expression.elseExpression.totalOperation.person!==null)
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
		}
		if(expression.elseExpression.set!==null){
			string9='''func else (s *«model.name»)'''+expression.name.toFirstUpper+"("+expression.elseExpression.set.compileMessageOperateGo+",ctx contractapi.TransactionContextInterface){"
			if(expression.elseExpression.totalOperation.person!==null)
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
		}
		var onlyOne='''bool one=true;
		if(one){
		'''
		var string10=''
		var string11=''
		var string12=''''''
		var string13=''
		if(expression.totalCondition!==null)
			string10+='if(!('+expression.totalCondition.compileTotalConditionGo(model)+')){'
		string11+=expression.elseExpression.totalOperation.compileTotalOperationGo(model)
		if(expression.totalCondition!==null)
			string13="}"
		if(expression.totalCondition!==null)
			string12="}"
		string8='''
			«string9»
				«string9_1»
				«onlyOne»
				«string10»
					«string11»
				«string13»
			«string12»
		'''
		code+='''
			«string8»
			one=false;
			}
		'''
		return code
		}
	}
		
	def dispatch compileManyRuleExpressionGo(ExclusiveExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileExclusiveRuleExpressionGo(model)»
		«ENDFOR»
	'''

	
	def compileExclusiveRuleExpressionGo(RuleExpression expression,Model model){
		var String code=''''''
		var string1=''
		var string1_1=''
		var link=''
		var setDateString1=''
		var setDateString2=''
		if(expression.setdate!==null){
			setDateString1='_year uint16, _months uint8, _days uint8, _hours uint8, _minutes uint8'
			setDateString2=expression.setdate.message+'=convertToTimestamp(_year,_months,_days,_hours,_minutes);'
		}
		if(expression.setdate!==null&&expression.set!==null){
			link=','
		}
		if(expression.repeat===null){
		if( expression.set===null){
			if(setDateString1 == '') 
				string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+"ctx contractapi.TransactionContextInterface) {"
			else string1 = '''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+",ctx contractapi.TransactionContextInterface) {"
			//string1="func "+expression.name+"("+setDateString1+") {"
			if(expression.totalOperation.person!==null)
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"		
		}
		if(expression.set!==null){
			string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+link+expression.set.compileMessageOperateGo+",ctx contractapi.TransactionContextInterface) {"
			if(expression.totalOperation.person!==null)
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string1_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
		}
		}
		if(expression.repeat!==null){
		if( expression.set===null){
			if(setDateString1 == '') 
				string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+"ctx contractapi.TransactionContextInterface) {"
			else string1 = '''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+",ctx contractapi.TransactionContextInterface) {"
			if(expression.totalOperation.person!==null)
				string1_1="if OnlyState(s.contractState){"
			else if(expression.totalOperation.person2!==null)
				string1_1="if OnlyState(s.contractState){"
			else
				string1_1="if OnlyState(s.contractState){"		
		}
		if(expression.set!==null){
			string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+link+expression.set.compileMessageOperateGo+",ctx contractapi.TransactionContextInterface) {"
			if(expression.totalOperation.person!==null)
				string1_1="if OnlyState(s.contractState){"
			else if(expression.totalOperation.person2!==null)
				string1_1="if OnlyState(s.contractState){"
			else
				string1_1="if OnlyState(s.contractState){"
		}
		}	
		var string2=''''''
		if(expression.set!==null)
			string2+=expression.set.compileMessageOperateThingGo
		var string3=''
		if(expression.totalCondition!==null){
			if(expression.totalCondition.condition!==null){
				string3 += expression.totalCondition.condition.condition.compileTrueConditionGo3(model)	
			}
			if(expression.totalCondition.andorcondition!==null){
				for(i: expression.totalCondition.andorcondition){
					if(i.link=="and")
						if(i.condition!==null)
					 		string3 += i.condition.condition.compileTrueConditionGo3(model)
					else if(i.link=="or")
							string3 += i.condition.condition.compileTrueConditionGo3(model)
				}
			}
			string3=string3+'if('+expression.totalCondition.compileTotalConditionGo(model)+'){'
		}
			
		var string4=''''''	
		string4+=expression.totalOperation.compileTotalOperationGo(model)
		
		var string5=''''''
		if(expression.subExpression!==null){
			for(i: expression.subExpression){
				var string6=''
				var string7=''''''
				var string8=''
				var string9=''
				if(i.totalExpression.totalCondition!==null)
					string6+='if('+i.totalExpression.totalCondition.compileTotalConditionGo(model)+'){'
				string7+=i.totalExpression.totalOperation.compileTotalOperationGo(model)
				if(i.totalExpression.elseExpression!==null)
					string8='''else{ 
						«i.totalExpression.elseExpression.totalOperation.compileTotalOperationGo(model)»
						}'''
				if(i.totalExpression.totalCondition!==null)
					string9="}"
				string5+='''
				 	«string6»
				 		«string7»
				 	«string8»
				 	«string9»
				'''
				}
			}
		
		var string6="ChangeRule(\""+expression.name+"\",s);"
		var string7=''''''
		if(expression.totalCondition!==null)
			string7="}"	
		
		code+='''
		«string1»
			«string1_1»
			«setDateString2»
			«string2»
			«string3»
				«string4»
				«string5»
				«string6»
			«string7»
		}
		}
		'''
		
		if(expression.elseExpression===null){
			return code
		}
	
		if(expression.elseExpression!==null){
			var string8=''''''
			var string9=''
			var string9_1=''
	
		if(expression.elseExpression.set===null){
			string9='''func else (s *«model.name»)'''+expression.name.toFirstUpper+"(ctx contractapi.TransactionContextInterface){"
			if(expression.elseExpression.totalOperation.person!==null)
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
		}
		if(expression.elseExpression.set!==null){
			string9='''func else (s *«model.name»)'''+expression.name.toFirstUpper+"("+expression.elseExpression.set.compileMessageOperateGo+",ctx contractapi.TransactionContextInterface){"
			if(expression.elseExpression.totalOperation.person!==null)
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string9_1="if OnlyState(s.contractState)&&!s.functionStatus["+'"'+expression.name+'"'+"]{"
		}
		var onlyOne='''bool one=true;
		if(one){
		'''
		var string10=''
		var string11=''
		var string12=''''''
		var string13=''
		if(expression.totalCondition!==null)
			string10+='if(!('+expression.totalCondition.compileTotalConditionGo(model)+')){'
		string11+=expression.elseExpression.totalOperation.compileTotalOperationGo(model)
		if(expression.totalCondition!==null)
			string13="}"
		if(expression.totalCondition!==null)
			string12="}"
		string8='''
			«string9»
				«string9_1»
				«onlyOne»
				«string10»
					«string11»
				«string13»
			«string12»
		'''
		code+='''
			«string8»
			one=false;
			}
		'''
		return code
		}
	}
	
	
	def dispatch compileManyRuleExpressionGo(AdditionExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileAdditionalRuleExpressionGo(model)»
		«ENDFOR»
	'''
	
	def compileAdditionalRuleExpressionGo(RuleExpression expression,Model model){
		var String code=''''''
		var string1=''
		var string1_1=''
		var link=''
		var setDateString1=''
		var setDateString2=''
		if(expression.setdate!==null){
			setDateString1='_year uint16, _months uint8, _days uint8, _hours uint8, _minutes uint8'
			setDateString2=expression.setdate.message+'=convertToTimestamp(_year,_months,_days,_hours,_minutes);'
		}
		if(expression.setdate!==null&&expression.set!==null){
			link=','
		}
		if(expression.repeat===null){
		if( expression.set===null){
			if(setDateString1 == '') 
				string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+"ctx contractapi.TransactionContextInterface) {"
			else string1 = '''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+",ctx contractapi.TransactionContextInterface) {"
			if(expression.totalOperation.person!==null)
				string1_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string1_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string1_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"		
		}
		if(expression.set!==null){
			string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+link+expression.set.compileMessageOperateGo+",ctx contractapi.TransactionContextInterface) {"
			if(expression.totalOperation.person!==null)
				string1_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string1_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string1_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
		}
		}
		if(expression.repeat!==null){
		if( expression.set===null){
			if(setDateString1 == '') 
				string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+"ctx contractapi.TransactionContextInterface) {"
			else string1 = '''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+",ctx contractapi.TransactionContextInterface) {"
			
			if(expression.totalOperation.person!==null)
				string1_1="if true{"
			else if(expression.totalOperation.person2!==null)
				string1_1="if true{"
			else
				string1_1="if true{"		
		}
		if(expression.set!==null){
			string1='''func (s *«model.name»)'''+expression.name.toFirstUpper+"("+setDateString1+link+expression.set.compileMessageOperateGo+",ctx contractapi.TransactionContextInterface) {"
			if(expression.totalOperation.person!==null)
				string1_1="if true{"
			else if(expression.totalOperation.person2!==null)
				string1_1="if true{"
			else
				string1_1="if true{"
		}
		}	
		var string2=''''''
		if(expression.set!==null)
			string2+=expression.set.compileMessageOperateThingGo
		var string3=''
		if(expression.totalCondition!==null){
			if(expression.totalCondition.condition!==null){
				string3 += expression.totalCondition.condition.condition.compileTrueConditionGo3(model)	
			}
			if(expression.totalCondition.andorcondition!==null){
				for(i: expression.totalCondition.andorcondition){
					if(i.link=="and")
						if(i.condition!==null)
					 		string3 += i.condition.condition.compileTrueConditionGo3(model)
					else if(i.link=="or")
							string3 += i.condition.condition.compileTrueConditionGo3(model)
				}
			}
				
				
//			else string3 += expression.totalCondition.condition.condition.compileTrueConditionGo3(model)
			
			string3=string3+'if('+expression.totalCondition.compileTotalConditionGo(model)+'){'
		}
			
		var string4=''''''	
		string4+=expression.totalOperation.compileTotalOperationGo(model)
		
		var string5=''''''
		if(expression.subExpression!==null){
			for(i: expression.subExpression){
				var string6=''
				var string7=''''''
				var string8=''
				var string9=''
				if(i.totalExpression.totalCondition!==null)
					string6+='if('+i.totalExpression.totalCondition.compileTotalConditionGo(model)+'){'
				string7+=i.totalExpression.totalOperation.compileTotalOperationGo(model)
				if(i.totalExpression.elseExpression!==null)
					string8='''else{ 
						«i.totalExpression.elseExpression.totalOperation.compileTotalOperationGo(model)»
						}'''
				if(i.totalExpression.totalCondition!==null)
					string9="}"
				string5+='''
				 	«string6»
				 		«string7»
				 	«string8»
				 	«string9»
				'''
				}
			}
		
		var string6="ChangeRule(\""+expression.name+"\",s);"
		var string7=''''''
		if(expression.totalCondition!==null)
			string7="}"	
		
		code+='''
		«string1»
			«string1_1»
			«setDateString2»
			«string2»
			«string3»
				«string4»
				«string5»
				«string6»
			«string7»
		}
		}
		'''
		
		if(expression.elseExpression===null){
			return code
		}
	
		if(expression.elseExpression!==null){
			var string8=''''''
			var string9=''
			var string9_1=''
		if(expression.elseExpression.set===null){
			string9='''func else (s *«model.name»)'''+expression.name.toFirstUpper+"(ctx contractapi.TransactionContextInterface){"
			if(expression.elseExpression.totalOperation.person!==null)
				string9_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string9_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string9_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
		}
		if(expression.elseExpression.set!==null){
			string9='''func else (s *«model.name»)'''+expression.name.toFirstUpper+"("+expression.elseExpression.set.compileMessageOperateGo+",ctx contractapi.TransactionContextInterface){"
			if(expression.elseExpression.totalOperation.person!==null)
				string9_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
			else if(expression.totalOperation.person2!==null)
				string9_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
			else
				string9_1="if !s.functionStatus["+'"'+expression.name+'"'+"]{"
		}
		var onlyOne='''bool one=true;
		if(one){
		'''
		var string10=''
		var string11=''
		var string12=''''''
		var string13=''
		if(expression.totalCondition!==null)
			string10+='if(!('+expression.totalCondition.compileTotalConditionGo(model)+')){'
		string11+=expression.elseExpression.totalOperation.compileTotalOperationGo(model)
		if(expression.totalCondition!==null)
			string13="}"
		if(expression.totalCondition!==null)
			string12="}"
		string8='''
			«string9»
				«string9_1»
				«onlyOne»
				«string10»
					«string11»
				«string13»
			«string12»
		'''
		code+='''
			«string8»
			one=false;
			}
		'''
		return code
		}
	}
	
	
	def compileMessageOperateGo(messageExpression operation){
		return operation.everymassage.defineEveryMessageGo
	
	}
	
	def defineEveryMessageGo(EList<everyMessage> list){
		var isFirst = true
		var result = ""
		for (i : list) {
		    if (!isFirst) {
		        result += ', '
		    }
		    if(i.type=="bool"||i.type=='address'||i.type=='string')
		    	result += "_"+i.message+" " +i.type
		    else if(i.type=='ufixed')  result += "_"+i.message+" " +'float64'
		    else if(i.type=='uint')  result += "_"+i.message+" " +'int64'
		    isFirst = false
		}
		return result;
	}
	
	def compileMessageOperateThingGo(messageExpression operation)'''
		«FOR i : operation.everymassage»
		 «IF i.symbol ===null»
			s.contractMessage.«i.message.toFirstUpper» = _«i.message»;
		«ELSE»
			«i.message» := _«i.message»;	
		«ENDIF»
		«ENDFOR»   
	'''
	
	def compileTotalConditionGo(TotalCondition condition,Model model){
		var String myString = ''
		if(condition.condition!==null)
			if(condition.condition.no!==null)
			myString=myString+'!'+condition.condition.condition.compileTrueConditionGo2(model)
			else
			myString=myString+condition.condition.condition.compileTrueConditionGo2(model)
		if(condition.linkCondition!==null)
			if(condition.linkCondition.no!==null)
				myString=myString+'!'+'s.'+condition.linkCondition.linkCondition.name.toFirstUpper+'(ctx)'
			else myString=myString+'s.'+condition.linkCondition.linkCondition.name.toFirstUpper+'(ctx)'
		for(i: condition.andorcondition){
			if(i.link=="and")
				myString=myString+'&&'
			else myString=myString+'||'
			if(i.condition!==null)
			if(i.condition.no!==null)
				myString=myString+'!'+i.condition.condition.compileTrueConditionGo2(model)
			else
				myString=myString+i.condition.condition.compileTrueConditionGo2(model)
			if(i.linkCondition!==null)
				if(i.linkCondition.no!==null)
					myString=myString+'!'+'s.'+i.linkCondition.linkCondition.name.toFirstUpper+'(ctx)'
				else
					myString=myString+'s.'+i.linkCondition.linkCondition.name.toFirstUpper+'(ctx)'
		}
		return myString
	}
	
	def compileTotalOperationGo(TotalOperation operation,Model model){
		var code=''''''
		var string1=''''''
		string1+='''
		«IF operation.timePoint!==null»
			«operation.timePoint.compileTimePoint»
		«ENDIF»
		'''
		if(operation.failresult!==null){
		string1+='''
			bool completed«con» = false;
			«IF operation.firstOperation!==null»
				«operation.firstOperation.compileTrueOperateGo(model)»
			«ENDIF»
			«IF operation.linkOperation!==null»
				«operation.linkOperation.compilelinkOperateGo»
			«ENDIF»
			«FOR i: operation.andor»
				«IF i.firstOperation!==null»
					«i.firstOperation.compileTrueOperateGo(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperateGo»
				«ENDIF»
			«ENDFOR»
			completed«con» = true;
			'''
			if(operation.timePoint!==null)
				string1+='}'
			string1+='''
			if(!completed«con»){
				«operation.failresult.compileFailResult(model)»
			}
			'''

			con=con+1
		}
		if(operation.failresult===null){
			string1+='''
			«IF operation.firstOperation!==null»
				«operation.firstOperation.compileTrueOperateGo(model)»
			«ENDIF»
			«IF operation.linkOperation!==null»
				«operation.linkOperation.compilelinkOperateGo»
			«ENDIF»
			«FOR i: operation.andor»
				«IF i.firstOperation!==null»
					«i.firstOperation.compileTrueOperateGo(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperateGo»
				«ENDIF»
			«ENDFOR»
			'''
			if(operation.timePoint!==null)
				string1+='}'
		}
		var string2=''''''	
		for(i:operation.thenoperation){
			string2+='''
			«IF i.timePoint!==null»
				«i.timePoint.compileTimePoint»
			«ENDIF»'''
			if(i.failresult!==null){
				string2+='''
				bool completed«con» = false;
				«IF i.followingOperation!==null»
					«i.followingOperation.compileTrueOperateGo(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperateGo»
				«ENDIF»
				«FOR j: i.andor»
					«IF j.firstOperation!==null»
						«j.firstOperation.compileTrueOperateGo(model)»
					«ENDIF»
					«IF j.linkOperation!==null»
						«j.linkOperation.compilelinkOperateGo»
					«ENDIF»
				«ENDFOR»
				completed«con» = true;
				'''
				if(i.timePoint!==null)
					string2+='}'
				string2+='''
				if(!completed«con»){
					«i.failresult.compileFailResult(model)»
				}
				'''
				con=con+1
			}
			
			if(i.failresult===null)
			string2+='''
				«IF i.followingOperation!==null»
					«i.followingOperation.compileTrueOperateGo(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperateGo»
				«ENDIF»
				«FOR j: i.andor»
					«IF j.firstOperation!==null»
						«j.firstOperation.compileTrueOperateGo(model)»
					«ENDIF»
					«IF j.linkOperation!==null»
						«j.linkOperation.compilelinkOperateGo»
					«ENDIF»
				«ENDFOR»
			'''
			if(i.timePoint!==null)
					string2+='}'
		}
		code+='''
		«string1»
		«string2»
		'''
		return code
	}
	
	
	def compilelinkOperateGo(OperateLink link)'''
		s.«link.name.toFirstUpper»(ctx);
	'''
	

	def dispatch compileTrueOperateGo(transferExpression operation, Model model){  

		var result = ""
    
    	// 反序列化 Seller
    	result += operation.person.name + '''Bytes, _ := ctx.GetStub().GetState("''' + operation.person.name + '''")
	            // 将获取到的 token 数据反序列化为 token 结构体
	            var ''' + operation.person.name + ''' Person
	            json.Unmarshal(''' + operation.person.name + '''Bytes, &''' + operation.person.name + ''')
	        '''
		
//		if(operation.value.compileExpression.startsWith("Service.")){
//			 result += 'ServiceBytes, _ := ctx.GetStub().GetState("Service")' + '''
//	            // 将获取到的 token 数据反序列化为 token 结构体
//	            var Service token
//	            json.Unmarshal(ServiceBytes, &Service)
//	        '''
//	            
//	            // 将 Service.price 转换为 Service.Price
//	           
//	            result +='transferTo(ctx,'+operation.person.name+','+"Service." + operation.value.compileExpression.substring(8).toFirstUpper+')'+';'
//    			return result
//			
//		}
//		
		val variables = parseExpression(operation.value.compileExpression)
		
		var foundToken = 0
		// 遍历每个变量，生成相应的反序列化代码
	    for (variable : variables) {
	    	var modifiedVariable = variable 
	        
	        // 如果是 ContractMessage 中的字段，前缀加 s.contractMessage
	        var foundContractMessage = false
	        for (key : model.contractMessage.message) {
	            if (key.type.name == variable) {
	                foundContractMessage = true
	            }
	        }

	        // 如果是 Token 类型的变量，进行反序列化
	        
	        if (!foundContractMessage) {
	        	for(object:model.objects){
	        		for (key : object.objectExpression.keyValue) {
	        			var modifiedKeyName = key.name 
		                if (key.name == variable) {
		                    foundToken += 1
		                }
	            	}
	        	}   
	        }
	       
	        
	        // 如果是未知变量，则直接使用原变量名
//	        if (!foundToken && !foundContractMessage) {
//            	result += modifiedVariable + "\n"
//        	}
	    }
		 if (foundToken>=1) {
	        	result += 'ServiceBytes, _ := ctx.GetStub().GetState("Service")' + "\n"
	            result += "// 将获取到的 token 数据反序列化为 token 结构体\n"
	            result += 'var Service token\n'
	            result += "json.Unmarshal(ServiceBytes, &Service)\n"
	        }
	    // transferTo 调用
//	    string += 'transferTo(ctx,' + operation.person.name + ',' + valueExpr + ')'+ ';'
//	    
//	    return string
// transferTo 调用
		
		val valueExpr = processExpressionFloat64(operation.value.compileExpression, model)
    	result += 'TransferTo(ctx, ' + operation.person.name + ', ' + valueExpr + ');\n'
    
    	return result
//	    result += 'transferTo(ctx, ' + operation.person.name + ', ' + operation.value.compileExpression + ');\n'
//	    
//	    return result

	}
	
	
	def processExpression(String expression, Model model) {
	    println("Processing expression: " + expression)
    // 定义一个包含常见运算符的集合
    	val operators = Arrays::asList('+', '-', '*', '/')
	    // 使用正则表达式提取变量名和运算符
	    val regex = "(\\w+\\.\\w+|\\w+|\\+|\\-|\\*|\\/|\\s+)"  // 提取变量、运算符
	    val pattern = Pattern.compile(regex)  // 编译正则表达式
	    val matcher = pattern.matcher(expression)  // 创建匹配器
	
	    val processedExpression = new StringBuilder()  // 用来存储最终处理后的表达式
	    var lastEndIndex = 0  // 上次匹配结束的索引
	
	    // 遍历匹配的部分
	    while (matcher.find()) {
	        val match = matcher.group(0)  // 获取当前匹配的文本
	        
	        
		        // 如果是 s.contractMessage. 并且后跟大写字母开头的字段，直接添加
	        if (match.matches("^s\\.contractMessage\\.[A-Z]\\w*")) {
	            processedExpression.append(match)
	        }
	        
			else if (match.matches("\\w+\\.\\w+")) {
            // 使用 Xtend 的替换方式处理字段，确保字段名的第二部分首字母大写
	            val processedVariable = match.split("\\.") // 将字符串按 "." 分割
	            if (processedVariable.size >= 2) { // 确保分割后有至少两部分
	                val capitalizedField = processedVariable.get(1).toFirstUpper // 第二部分（字段名）首字母大写
	                val result = processedVariable.get(0) + "." + capitalizedField
	                processedExpression.append(result)
	            } else {
	                // 如果分割失败，直接添加原始文本
	                processedExpression.append(match)
	            }
        	}
	        // 如果匹配的是变量，进行处理
	        else if (match.matches("\\w+")) {
	            val processedVariable = processVariable(match, model)
	            processedExpression.append(processedVariable)
	        }
	        // 如果匹配的是运算符，则直接添加
	        else if (operators.contains(match)) {
	            processedExpression.append(match)
	        }
	        // 其他情况
	        else {
	            processedExpression.append(match)
	        }
	        
	        lastEndIndex = matcher.end()
	    }
	

	    return processedExpression.toString()
	}
	
	
	
	def processExpressionInt64(String expression, Model model) {
	    println("Processing expression: " + expression)
    // 定义一个包含常见运算符的集合
    	val operators = Arrays::asList('+', '-', '*', '/')
	    // 使用正则表达式提取变量名和运算符
	    val regex = "(\\w+\\[.*?\\]|\\w+\\.\\w+|\\w+|\\+|\\-|\\*|\\/|\\s+)" // 提取变量、运算符
	    val pattern = Pattern.compile(regex)  // 编译正则表达式
	    val matcher = pattern.matcher(expression)  // 创建匹配器
	
	    val processedExpression = new StringBuilder()  // 用来存储最终处理后的表达式
	    var lastEndIndex = 0  // 上次匹配结束的索引
	
	    // 遍历匹配的部分
	    while (matcher.find()) {
	        val match = matcher.group(0)  // 获取当前匹配的文本
	         // 如果是带索引的变量，例如 functionFinishTime["rule3"]
	        if (match.matches("\\w+\\[.*?\\]")) {
	            processedExpression.append('int64(s.' + match + ')')
	        }
			else if (match.matches("\\w+\\.\\w+")) {
            // 使用 Xtend 的替换方式处理字段，确保字段名的第二部分首字母大写
	            val processedVariable = match.split("\\.") // 将字符串按 "." 分割
	            if (processedVariable.size >= 2) { // 确保分割后有至少两部分
	                val capitalizedField = processedVariable.get(1).toFirstUpper // 第二部分（字段名）首字母大写
	                val result = processedVariable.get(0) + "." + capitalizedField
	                processedExpression.append(result)
	            } else {
	                // 如果分割失败，直接添加原始文本
	                processedExpression.append(match)
	            }
        	}
	        // 如果匹配的是变量，进行处理
	        else if (match.matches("\\w+")) {
	            val processedVariable = processVariable(match, model)
	            processedExpression.append('int64('+processedVariable+')')
	        }
	        // 如果匹配的是运算符，则直接添加
	        else if (operators.contains(match)) {
	            processedExpression.append(match)
	        }
	        // 其他情况
	        else {
	            processedExpression.append('int64('+match+')')
	        }
	        
	        lastEndIndex = matcher.end()
	    }
	
	    // 返回处理后的表达式
	    return processedExpression.toString()
	}
	
	
	def processExpressionFloat64(String expression, Model model) {
	    println("Processing expression: " + expression)
    // 定义一个包含常见运算符的集合
    	val operators = Arrays::asList('+', '-', '*', '/')
	    // 使用正则表达式提取变量名和运算符
	    val regex = "(\\w+\\.\\w+|\\w+|\\+|\\-|\\*|\\/|\\s+)"  // 提取变量、运算符
	    val pattern = Pattern.compile(regex)  // 编译正则表达式
	    val matcher = pattern.matcher(expression)  // 创建匹配器
	
	    val processedExpression = new StringBuilder()  // 用来存储最终处理后的表达式
	    var lastEndIndex = 0  // 上次匹配结束的索引
	
	    // 遍历匹配的部分
	    while (matcher.find()) {
	        val match = matcher.group(0)  // 获取当前匹配的文本
			if (match.matches("\\w+\\.\\w+")) {
            // 使用 Xtend 的替换方式处理字段，确保字段名的第二部分首字母大写
	            val processedVariable = match.split("\\.") // 将字符串按 "." 分割
	            if (processedVariable.size >= 2) { // 确保分割后有至少两部分
	                val capitalizedField = processedVariable.get(1).toFirstUpper // 第二部分（字段名）首字母大写
	                val result = processedVariable.get(0) + "." + capitalizedField
	                processedExpression.append('float64('+result+')')
	            } else {
	                // 如果分割失败，直接添加原始文本
	                processedExpression.append('float64('+match+')')
	            }
        	}
	        // 如果匹配的是变量，进行处理
	        else if (match.matches("\\w+")) {
	            val processedVariable = processVariable(match, model)
	            processedExpression.append('float64('+processedVariable+')')
	        }
	        // 如果匹配的是运算符，则直接添加
	        else if (operators.contains(match)) {
	            processedExpression.append(match)
	        }
	        // 其他情况
	        else {
	            processedExpression.append('float64('+match+')')
	        }
	        
	        lastEndIndex = matcher.end()
	    }
	
	    // 返回处理后的表达式
	    return processedExpression.toString()
	}

	def processVariable(String variable, Model model) {
	    // 检查变量是属于 contractMessage 还是 token，并进行相应的转换
	    var processedVariable = variable
	    var found = false
	    
	    if (variable.startsWith("o ")) {
        processedVariable = variable.substring(2)  // 去掉 "o "（从第3个字符开始）
    	}
	
	    // 查找 contractMessage
	    for (contractMessage : model.contractMessage.message) {
	        if (contractMessage.type.name == variable) {
	            processedVariable = "s.contractMessage." + contractMessage.type.name.toFirstUpper
	            found = true
	        }
	    }
	
	    // 如果没有找到 contractMessage，则检查 token
	    if (!found) {
	        for (object : model.objects) {
	            for (key : object.objectExpression.keyValue) {
	                if (key.name == variable) {
	                    processedVariable = "Service." + key.name.toFirstUpper
	                    found = true
	                    
	                }
	            }
	           
	        }
	    }
	
	    // 返回处理后的变量名（已转为对应格式）
	    return processedVariable
	}
	
	def parseExpression(String expression) {
		println("Extracting variables from expression: " + expression)

	    val regex = "[a-zA-Z_][a-zA-Z0-9_]*"  // 使用正则表达式来匹配变量名
	    val pattern = Pattern.compile(regex)  // 编译正则表达式
	    val matcher = pattern.matcher(expression)  // 创建匹配器
	
	    val vars = new ArrayList<String>()  // 创建一个 ArrayList 来存储变量名
	
	    while (matcher.find()) {
	        val varName = matcher.group(0)  // 获取匹配的变量名
	        println("Found variable: " + varName)
	        vars.add(varName)  // 将变量名添加到列表中
	    }
	    
	    return vars  // 返回变量名列表
	}
	
	
	def dispatch compileTrueOperateGo(changeExpression operation, Model model){
		return operation.compileChangeExpressionGo(model)
	}
	

	
	def dispatch compileChangeExpressionGo(otherchange expression,Model model){
		var string=''
		
		var changething = expression.changeThing.compileExpression
		var foundContractMessage = false
		
		for (key : model.contractMessage.message) {
            if (key.type.name == changething) {
                foundContractMessage = true
                changething = "s.contractMessage."+changething.toFirstUpper
            }
	      }
	      
        var foundToken = 0
        if (changething.startsWith("Service.")){
        	foundToken+=1
			val suffix = changething.substring(8) // 从第 8 个字符开始截取后续部分
		    // 将第一个字母大写
		    changething = "Service." + suffix.toFirstUpper
		}
        if (!foundContractMessage) {
        	for(object:model.objects){
        		for (key : object.objectExpression.keyValue) {
        			var modifiedKeyName = key.name 
	                if (key.name == changething) {
	                	changething = "Service."+changething.toFirstUpper
	                    foundToken += 1
	                }
            	}
        	}   
        }
        	   
		val variables = parseExpression(expression.changeResult.compileExpression)
		
		// 遍历每个变量，生成相应的反序列化代码
	    for (variable : variables) {
	        // 如果是 Token 类型的变量，进行反序列化
        	for(object:model.objects){
        		for (key : object.objectExpression.keyValue) {
        			var modifiedKeyName = key.name 
	                if (key.name == variable) {
	                    foundToken+=1
	                }
            	}
        	}   
	       
	    }
	    
	    if (foundToken>=1) {
	        	string += 'ServiceBytes, _ := ctx.GetStub().GetState("Service")' + "\n"
	            string += "// 将获取到的 token 数据反序列化为 token 结构体\n"
	            string += 'var Service token\n'
	            string += "json.Unmarshal(ServiceBytes, &Service)\n"
	        }
		
// transferTo 调用
		if(expression.changeResult.compileExpressionGo2=="time.Now().Unix()"){
			val valueExpr = "time.Now().Unix()"
			string+=changething+'='+valueExpr
		}else{
			val valueExpr = processExpression(expression.changeResult.compileExpressionGo2, model)
	    	println("2210:expression.changeResult.compileExpressionGo:"+expression.changeResult.compileExpressionGo2)
	    	println("2209:valueExpr:"+valueExpr)
	    	string+=changething+'='+valueExpr
		}
		
//	    result += 'transferTo(ctx, ' + operation.person.name + ', ' + operation.value.compileExpression + ');\n'
//	    
//	    return result
		
		
		
		
		return string
	}
	
	def dispatch compileChangeExpressionGo(changeString expression,Model model){
		var string=''
		string=expression.changeThing.compileExpression+'=\"'+expression.changeResult.compileExpression+'\"'
		return string
	}
	
	
	
	def dispatch compileChangeExpressionGo(changeContract expression,Model model){
		var string=''
		string='s.contractState=\"'+expression.changeResult+'\";'
		return string
	}
	
	def dispatch compileChangeExpressionGo(changeRule expression,Model model){
		var string=''
		string='s.functionStatus[\"'+expression.changeThing.name+'\"]='+expression.changeResult+';'
		return string
	}
	
	def dispatch compileChangeExpressionGo(changeOther expression,Model model){
		var string=''
		string=expression.changeThing.compileSingleExpression+"="+expression.changeResult+';'
		return string
	}
	
	def dispatch compileChangeExpressionGo(changeAddress expression,Model model){
		var string=''
		string=expression.changeThing.compileExpression+'='+"("+expression.changeResult.compileExpression+');'
		return string
	}
	
	
	
	def compileRequireGo(Require require,Model model)'''
		«FOR i :  require.value»
			«IF i == "isTime"»
			    // 用于判断是否达到指定时间的函数
			    func IsTime(targetTime int64) bool {
			        return int64(time.Now().Unix()) >= targetTime;
			    }
			«ELSEIF i == "logic"»
				// 辅助函数，根据比较符号执行比较
				func Logic(a, b interface{}, op string) bool {
					switch a := a.(type) {
					case int:
						b, ok := b.(int)
						if !ok {
							return false
						}
						return Compare(float64(a), float64(b), op)
					case float64:
						b, ok := b.(float64)
						if !ok {
							return false
						}
						return Compare(a, b, op)
					case string:
						b, ok := b.(string)
						if !ok {
							return false
						}
						return CompareString(a, b, op)
					default:
						return false
					}
				}
				
				func Compare(a, b float64, op string) bool {
					switch op {
					case "<":
						return a < b
					case "<=":
						return a <= b
					case "==":
						return a == b
					case "!=":
						return a != b
					case ">":
						return a > b
					case ">=":
						return a >= b
					default:
						return false
					}
				}
				
				func CompareString(a, b string, op string) bool {
					switch op {
					case "<":
						return a < b
					case "<=":
						return a <= b
					case "==":
						return a == b
					case "!=":
						return a != b
					case ">":
						return a > b
					case ">=":
						return a >= b
					default:
						return false
					}
				}
			«ELSEIF i == "check"»
				func Check(contractName string) bool {
					return true;
				}
			«ELSEIF i == "isDone"»
			    // 检查某个功能是否已经执行
			    func IsDone(ctx contractapi.TransactionContextInterface, functionName string, s *«model.name») bool {
			    	return s.functionStatus[functionName]
			    }
«««			    func isDone(functionName string) bool {
«««			        return functionStatus[functionName];
«««			    }
			«ELSEIF i == "SetDate"»
			    // isLeapYear 判断给定的年份是否是闰年
			    func IsLeapYear(year uint16) bool {
			    	if year%4 != 0 {
			    		return false
			    	}
			    	if year%100 != 0 {
			    		return true
			    	}
			    	if year%400 != 0 {
			    		return false
			    	}
			    	return true
			    }
			    
			    // getDaysInMonth 根据给定的月份和年份返回该月的天数
			    func GetDaysInMonth(month uint8, year uint16) uint8 {
			    	switch month {
			    	case 1, 3, 5, 7, 8, 10, 12:
			    		return 31
			    	case 4, 6, 9, 11:
			    		return 30
			    	default:
			    		if IsLeapYear(year) {
			    			return 29
			    		}
			    		return 28
			    	}
			    }
			    
			    // convertToTimestamp 将给定的日期和时间转换为Unix时间戳
			    func ConvertToTimestamp(year uint16, month uint8, day uint8, hour uint8, minute uint8) uint64 {
			    	// 检查参数的有效性
			    	if year < 1970 || month < 1 || month > 12 || day < 1 || day > getDaysInMonth(month, year) || hour > 23 || minute > 59 {
			    		panic("Invalid input parameters")
			    	}
			    
			    	var timestamp uint64
			    	// 计算年份之前的天数
			    	for y := uint16(1970); y < year; y++ {
			    		if isLeapYear(y) {
			    			timestamp += 366 * 24 * 60 * 60
			    		} else {
			    			timestamp += 365 * 24 * 60 * 60
			    		}
			    	}
			    	// 计算月份之前的天数
			    	for m := uint8(1); m < month; m++ {
			    		timestamp += uint64(getDaysInMonth(m, year)) * 24 * 60 * 60
			    	}
			    	// 加上日、小时和分钟的秒数
			    	timestamp += uint64((day-1)*24*60*60 + hour*60*60 + minute*60)
			    
			    	return timestamp
			    }
			«ELSEIF i == "timeSub"»
			     // compareTimestamps 比较两个时间戳，并根据给定的操作符和自定义秒数返回布尔值
			     func CompareTimestamps(timestamp1 int64, timestamp2 int64, customSeconds int64, operator string) bool {
			     	// 计算两个时间戳的差值（取绝对值）
			     	var timeDifference int64
			     	if timestamp1 > timestamp2 {
			     		timeDifference = timestamp1 - timestamp2
			     	} else {
			     		timeDifference = timestamp2 - timestamp1
			     	}
			     
			     	// 判断操作符并执行相应的比较
			     	switch strings.TrimSpace(operator) {
			     	case ">":
			     		return timeDifference > customSeconds
			     	case "<":
			     		return timeDifference < customSeconds
			     	case "==":
			     		return timeDifference == customSeconds
			     	case "!==":
			     		return timeDifference != customSeconds
			     	default:
			     		panic("Invalid operator")
			     	}
			     }
			«ELSEIF i == "transfer"»
				// transferTo 用于给指定地址转账并执行模拟支付流程
				func TransferTo(ctx contractapi.TransactionContextInterface, recipient Person, amount float64) error {
					recipientMSPID := recipient.MSPID
					if recipientMSPID == "" {
						return fmt.Errorf("Invalid recipient address")
					}
					if amount <= 0 {
						return fmt.Errorf("Amount must be greater than zero")
					}
				
					// 获取调用者的余额
					clientMSPID, err := ctx.GetClientIdentity().GetMSPID()
					if err != nil {
						return fmt.Errorf("failed to get client MSPID: %v", err)
					}
					callerKey := fmt.Sprintf("account_%s", clientMSPID)
					callerBalanceBytes, err := ctx.GetStub().GetState(callerKey)
					if err != nil {
						return fmt.Errorf("failed to get caller balance: %v", err)
					}
					if callerBalanceBytes == nil {
						return fmt.Errorf("caller account does not exist")
					}
				
					// 获取接收方余额
					recipientKey := fmt.Sprintf("account_%s", recipientMSPID)
					recipientBalanceBytes, err := ctx.GetStub().GetState(recipientKey)
					if err != nil {
						return fmt.Errorf("failed to get recipient balance: %v", err)
					}
					if recipientBalanceBytes == nil {
						return fmt.Errorf("recipient account does not exist")
					}
					var callerBalance float64
					if err := json.Unmarshal(callerBalanceBytes, &callerBalance); err != nil {
						return fmt.Errorf("failed to unmarshal caller balance: %v", err)
					}
				
					// 确保调用方有足够的余额支付
					var recipientBalance float64
					if err := json.Unmarshal(recipientBalanceBytes, &recipientBalance); err != nil {
						return fmt.Errorf("failed to unmarshal recipient balance: %v", err)
					}
				
					if callerBalance < amount {
						return fmt.Errorf("insufficient balance in caller account")
					}
				
					// 扣除调用方的金额
					callerBalance -= amount
				
					// 增加接收方的金额
					recipientBalance += amount
				
					// 更新调用方余额
					updatedCallerBalanceBytes, err := json.Marshal(callerBalance)
					if err != nil {
						return fmt.Errorf("failed to marshal updated caller balance: %v", err)
					}
					if err := ctx.GetStub().PutState(callerKey, updatedCallerBalanceBytes); err != nil {
						return fmt.Errorf("failed to update caller balance: %v", err)
					}
					
					// 更新接收方的余额
					updatedRecipientBalanceBytes, err := json.Marshal(recipientBalance)
					if err != nil {
						return fmt.Errorf("failed to marshal updated recipient balance: %v", err)
					}
					if err := ctx.GetStub().PutState(recipientKey, updatedRecipientBalanceBytes); err != nil {
						return fmt.Errorf("failed to update recipient balance: %v", err)
					}
					
					return nil
				}
			«ELSEIF i == "compareString"»
				func CompareStrings(str1, str2 string) bool {
				    return str1 == str2
				}
			«ENDIF»
		    «ENDFOR»
		'''
	
	def dispatch compileManyRuleGo(AdditionExpression expression)''''''
	def dispatch compileManyRuleGo(RegularRuleExpression expression)''''''
	def dispatch compileManyRuleGo(ParallelExpression expression)''''''
	def dispatch compileManyRuleGo(ExclusiveExpression expression){
		var String code=''''''
		var String varvalue=""
		var String string01='var myBoolean'+exclu+' bool := true'
		var String string02=''''''
		var j=0
		for(rule : expression.ruleExpression){
			if(expression.ruleExpression.get(j).set!==null)
			varvalue=varvalue+','+expression.ruleExpression.get(j).set.everymassage.defineMessage2
			j=j+1
		}
		var String string1=""
		string1=string1+"func exclusive"+exclu+" (_choice int"+ varvalue+")"+"{"
		var String string2=""
		string2="var choice int := _choice;"
		var i=0
		var String code2=''''''
		for( rule : expression.ruleExpression){
		var String string3=""
		var String string4=""
		string3="if(choice=="+i+"&& myBoolean"+exclu+"){"
		if(expression.ruleExpression.get(i).set!==null)
			string4=expression.ruleExpression.get(i).name+"("+expression.ruleExpression.get(i).set.everymassage.defineMessage+");"
		else
			string4=expression.ruleExpression.get(i).name+"();"
		code2+='''
		«string3»
			«string4»
			myBoolean«exclu» = false
			}
		'''
		i=i+1
		}
		exclu=exclu+1
		code+='''
		«string01»
			«string02»
		«string1»
			«string2»
			«code2»
		}
		'''
		return code
}
	
		
	def compileSolidity(Model model)'''
		pragma solidity >=0.7.0 <0.9.0;
		«FOR name : model.packageName»
			import "./«name».sol";
		«ENDFOR»
		contract «model.name» «IF model.extendName !== null» is «model.extendName» «ENDIF» 
		{ 
			«IF model.contractMessage !== null»
				« model.contractMessage.compileMessage»
			«ENDIF»
			«FOR subject:model.subjects»
				«subject.compileSubject»
		    «ENDFOR»
			«FOR object: model.objects»
				«object.compileObject»
			«ENDFOR»
			«IF model.initialize !== null»
				«model.initialize.compileInit»
			«ENDIF»
			«FOR group : model.group»
				«group.compileGroup»
			«ENDFOR»
			constructor() {
				«FOR group : model.group»
				 	«FOR value :  group.value»
				 		«group.name».push(«value.name»);
		 	    	«ENDFOR»
				«ENDFOR»
			}
			event completedRule(address indexed person, string rulename);
			«IF model.operations !== null»
				«model.operations.compileOperation(model)»
			«ENDIF»
			«IF model.conditions !== null»
				«model.conditions.compileCondition(model)»
			«ENDIF»
			«FOR i : model.ruleStructures.manyRuleExpression»
				«i.compileManyRule»
			«ENDFOR»
			«IF model.ruleStructures!==null»
				«model.ruleStructures.compileRule(model)»
			«ENDIF»
			«IF model.require !== null »
				«model.require.compileRequire»
			«ENDIF»
		    modifier unDone(string memory functionName){
		        require(!(functionStatus[functionName]),"require do this function unsucessfully");
		        _;
		    }
			event ContractStateChange(string newState);
			// 自定义 modifier：根据字符串参数值允许或禁止执行
			modifier onlyState(string memory State) {
			    require(compareStrings(State, "start") || compareStrings(State, "restart"), "Not allowed in this state");
			     emit ContractStateChange(State);
			    _;
			}
			// 辅助函数，比较两个字符串是否相等
			function compareStrings(string memory a, string memory b) internal pure returns (bool) {
			    return (keccak256(abi.encodePacked(a)) == keccak256(abi.encodePacked(b)));
			}
			
			//每个条款固定调用函数，条款执行后更改条款状态，记录事件和时间
			function changeRule(string memory ruleName) internal {
				functionStatus[ruleName] = true;
				functionFinishTime[ruleName]=block.timestamp;
				emit completedRule(msg.sender,ruleName);
		}
		}
	'''
	
	def dispatch compileManyRule(AdditionExpression expression)''''''
	def dispatch compileManyRule(RegularRuleExpression expression)''''''
	def dispatch compileManyRule(ParallelExpression expression)''''''
	def dispatch compileManyRule(ExclusiveExpression expression){
		var String code=''''''
		var String varvalue=""
		var String string01='mapping(uint => string) public Exclusive'+exclu+' ;'
		var String string02=''''''
		string02+='function initializeExclusive'+exclu+'() public {\n'
		var index=0
		for( rule : expression.ruleExpression){
			string02+='Exclusive'+exclu+'['+index+']'+'=\"'+rule.name+'\";\n'
			index=index+1
		}
		string02+='}'
		var j=0
		for(rule : expression.ruleExpression){
			if(expression.ruleExpression.get(j).set!==null)
			varvalue=varvalue+','+expression.ruleExpression.get(j).set.everymassage.defineMessage2
			j=j+1
		}
		var String string1=""
		string1=string1+"function exclusive"+exclu+" (uint _choice"+ varvalue+") public payable 
			only"+expression.ruleExpression.get(0).totalOperation.person.name+" {"
		var String string2=""
		string2="uint choice=_choice;"
		var i=0
		var String code2=''''''
		for( rule : expression.ruleExpression){
		var String string3=""
		var String string4=""
		string3="if(choice=="+i+")"
		if(expression.ruleExpression.get(i).set!==null)
			string4=expression.ruleExpression.get(i).name+"("+expression.ruleExpression.get(i).set.everymassage.defineMessage+");"
		else
			string4=expression.ruleExpression.get(i).name+"();"
		code2+='''
		«string3»
			«string4»
		'''
		i=i+1
		}
		exclu=exclu+1
		code+='''
		«string01»
			«string02»
		«string1»
			«string2»
			«code2»
		}
		'''
		return code
	}
	
	def String defineMessage2(EList<everyMessage> list){
		var result = ""
		for (i : list) {
		        result = ', '+i.type+' _'+i.message
		}
		return result;
	}
	
	def defineMessage(EList<everyMessage> list){
		var isFirst = true
		var result = ""
		for (i : list) {
		    if (!isFirst) {
		        result = ', '+result
		    }
		    result += i.message
		    isFirst = false
		}
		return result;
	}
	
	def compileRequire(Require require)'''
		«FOR i :  require.value»
			«IF i == "isTime"»
			    // 用于判断是否达到指定时间的函数
			    function isTime(uint256 targetTime) internal view returns (bool) {
			        return block.timestamp >= targetTime;
			    }
			«ELSEIF i == "logic"»
				// 辅助函数，根据比较符号执行比较
				    function compare(uint256 a, uint256 b, string memory op) internal pure returns (bool) {
				        if (compareStrings(op, ">")) {
				            return a > b;
				        } else if (compareStrings(op, "<")) {
				            return a < b;
				        } else if (compareStrings(op, ">=")) {
				            return a >= b;
				        } else if (compareStrings(op, "<=")) {
				            return a <= b;
				        } else if (compareStrings(op, "==")) {
				            return a == b;
				        } else if (compareStrings(op, "!=")) {
				            return a != b;
				        }
				        revert("Invalid operator.");
				    }
				
				
				    // 只有当满足条件 valueA > valueB 时，才能执行 setValue 函数
				    function logic(uint256 valueA, uint256 valueB, string memory symbol) internal pure returns (bool) {
				        return compare(valueA, valueB, symbol);
				    }
				    
				    function logic(string memory valueA, string memory valueB, string memory symbol) internal pure returns (bool) {
		    	    	return compareStrings(valueA, valueB);
		    	    }
			«ELSEIF i == "check"»
				function check(string memory contractName) public view returns (bool){
					return true;
				}
			«ELSEIF i == "isDone"»
			    // 检查某个功能是否已经执行
			    function isDone(string memory functionName) internal view returns (bool) {
			        return functionStatus[functionName];
			    }
			«ELSEIF i == "isCompleted"»
				function isCompleted(string memory ruleName) internal view returns (bool){
					return true;
				}
			«ELSEIF i == "SetDate"»
				    function isLeapYear(uint16 year) internal pure returns (bool) {
				        if (year % 4 != 0) {
				            return false;
				        }
				        if (year % 100 != 0) {
				            return true;
				        }
				        if (year % 400 != 0) {
				            return false;
				        }
				        return true;
				    }
				
				    function getDaysInMonth(uint8 month, uint16 year) internal pure returns (uint8) {
				        if (month == 1 || month == 3 || month == 5 || month == 7 || month == 8 || month == 10 || month == 12) {
				            return 31;
				        } else if (month == 4 || month == 6 || month == 9 || month == 11) {
				            return 30;
				        } else if (isLeapYear(year)) {
				            return 29;
				        } else {
				            return 28;
				        }
				    }
				
				    function convertToTimestamp(uint16 year, uint8 month, uint8 day, uint8 hour, uint8 minute) public pure returns (uint256) {
				        require(year >= 1970, "Year must be 1970 or later");
				        require(month >= 1 && month <= 12, "Invalid month");
				        require(day >= 1 && day <= getDaysInMonth(month, year), "Invalid day");
				        require(hour >= 0 && hour <= 23, "Invalid hour");
				        require(minute >= 0 && minute <= 59, "Invalid minute");
				        uint256 timestamp = 0;
				        for (uint16 y = 1970; y < year; y++) {
				            if (isLeapYear(y)) {
				                timestamp += 366 days;
				            } else {
				                timestamp += 365 days;
				            }
				        }
				        for (uint8 m = 1; m < month; m++) {
				            timestamp += uint256(getDaysInMonth(m, year)) * 1 days;
				        }
				        timestamp += uint256(day - 1) * 1 days;
				        timestamp += uint256(hour) * 1 hours;
				        timestamp += uint256(minute) * 1 minutes;
				        return timestamp;
				    }
			«ELSEIF i == "timeSub"»
			     function compareTimestamps(uint256 timestamp1, uint256 timestamp2, uint256 customSeconds, string memory operator) internal pure returns (bool) {
			            // 计算两个时间戳的差值（取绝对值，确保差值为正数）
			            uint256 timeDifference = timestamp1 - timestamp2;
			            if (keccak256(bytes(operator)) == keccak256(bytes(">"))) {
			                return timeDifference > customSeconds;
			            } else if (keccak256(bytes(operator)) == keccak256(bytes("<"))) {
			                return timeDifference < customSeconds;
			            } else if (keccak256(bytes(operator)) == keccak256(bytes("=="))) {
			                return timeDifference == customSeconds;
			            } else if (keccak256(bytes(operator)) == keccak256(bytes("!=="))) {
			                return timeDifference != customSeconds;
			            } else {
			                revert("Invalid operator");
			            }
			        }
			«ELSEIF i == "transfer"»
				event Transfer(address indexed from, address indexed to, uint256 amount);
				// 用于给指定地址转账
				function transferTo(address payable recipient, uint256 _amount) internal {
				    require(recipient != address(0), "Invalid recipient address");
				    require(_amount > 0, "Amount must be greater than zero");
				    recipient.transfer(_amount);
				    emit Transfer(msg.sender,recipient, _amount);
				}
			«ENDIF»
	    «ENDFOR»
	'''
	
	def compileMessage(ContractMessage message)'''
		«FOR i :  message.message»
			«IF i.type.type == "date"»
			uint256 public «i.type.name» = «i.value.compilevalue»;
			«ELSEIF i.type.type == "address"»
    			«i.type.type» public «i.type.name» = payable(«i.value.compilevalue»);
			«ELSEIF i.type.type == "duration"»
			uint256 public «i.type.name» = «i.value.compilevalue» ;
			«ELSEIF i.type.type == "ufixed" || i.type.type == "fixed" »
			uint256 public «i.type.name» = «i.value.compilevalue» ;
			«ELSEIF i.type.type != "date" && i.type.type != "address"»
				«i.type.type» public «i.type.name» = «i.value.compilevalue» ;«ENDIF»
	    «ENDFOR»
		string public ContractState="start";
		mapping(string => bool) public functionStatus;
		mapping(string => uint) public functionFinishTime;	
	'''

	def compileSubject(Subject subject) '''
«««	建立结构体
	struct «subject.name» {
		«IF subject.type != "CA"»
		string name;
		address payable account;
		«ENDIF»
		«IF subject.type == "CA"»
		string name;
		address payable account;
		uint256 key;
		uint256 year;
		«ENDIF»
		«IF subject.subjectExpression !== null»
			«subject.subjectExpression.compileSubjectExpression»
		«ENDIF»
	}
	'''
	
	def compileSubjectExpression(SubjectExpression SuE){'''
		«FOR key :  SuE.keyValue»
			«IF key.type == "date" || key.type == "ufixed"»
				uint256  «key.name» ;
				«ELSEIF key.type != "date"&&key.type != "ufixed"»
    				«key.type»   «key.name» ; «ENDIF»
    	«ENDFOR»
    '''
	}
	
	def compileObject(Object object)'''
	«««	建立结构体
	struct «object.name» {
		   «object.objectExpression.compileObjectExpression»
	}
	
	'''
	
	def compileObjectExpression(ObjectExpression ObE) {'''
		«FOR key :  ObE.keyValue»
			«IF key.type == "date" || key.type == "ufixed"»
				uint256  «key.name» ;
				«ELSEIF key.type != "date" || key.type != "ufixed"»
    				«key.type»   «key.name» ; «ENDIF»
    	«ENDFOR»
    '''
}
	
	def compileInit(Initialize init){'''
«««初始化主体
		«FOR sinit:init.inits»
			«sinit.subtype.name» public «sinit.name» = «sinit.subtype.name»(«sinit.value.compilevaluelist»);
		«ENDFOR»
«««建立相应的modifier
		«FOR sinit:init.inits»
			modifier only«sinit.name»(){
				require(msg.sender == «sinit.name».account,"Only «sinit.name» can access this.");
				 _; 
			}
		«ENDFOR»
«««初始化客体
		«FOR oinit:init.inito»
			«oinit.obtype.name» public «oinit.name» = «oinit.obtype.name»(«oinit.value.compilevaluelist»);
		«ENDFOR»
	'''		
	}
	
	def compilevaluelist(EList<Value> list){
		var isFirst = true
		var result = ""
		for (value : list) {
		    if (!isFirst) {
		        result += ', '
		    }
		    result += value.compilevalue
		    isFirst = false
		}
		return result
}
	
	def dispatch compilevalue(Address value){
		return 'payable('+value.value+')'
	}
	
	def dispatch compilevalue(AllNumber value){
		return value.number
	}
	
	def dispatch compilevalue(Now value){
		return 'block.timestamp'
	}
	
	def dispatch compilevalue(ThisDecimal value){
		return stringToDouble(value.value)*1000
		
	}
	
	def double stringToDouble(String str) {
    	return Double::parseDouble(str)
	}
	
	
	def dispatch compilevalue(url value){
		return value.value
	}
	
	def dispatch compilevalue(ThisString value){
		return '"'+value.value+'"'
	}
	
	def dispatch compilevalue(ThisBoolean value){
		return value.value
	}
	
	def dispatch compilevalue(Duration value){
		if(value.symbol=="months")
			return (value.value)*60*60*24*30
		else if(value.symbol=="days")
			return (value.value)*60*60*24
		else if(value.symbol=="mins")
			return (value.value)*60
		else if(value.symbol=="hours")
			return (value.value)*60*60
		else if(value.symbol=="years")
			return (value.value)*60*60*24*30*365
	}
	
	def dispatch compilevalue(ThisDate value){
		return toTimestamp(value.value.year,value.value.month,value.value.day,value.value.hour,value.value.min)
	}
	
	def dispatch compilevalue(Right value){
		return '"'+value.right.name+'"'
	}
	
	def compilevalue2(Address value){
		return 'payable('+value.value+')'
	}
	
	def compileGroup(Group group){
		var string=''''''
		var string1=''
		var boolean first=true
		for(i:group.value){
			if(!first){
			string1+=','+i.name
			}
			else {
				string1+=i.name
				first=false
			}
			}
		//string1=group.subtype.name+'[] public '+group.name+'=['+string1+'];'
		string1=group.subtype.name+'[] public '+group.name+';'
		var string2='''
		// Modifier：仅允许一堆结构体中的任何一个地址执行
		    modifier only«group.name»() {
		        bool found = false;
		        for (uint256 i = 0; i < «group.name».length; i++) {
		            if («group.name»[i].account == msg.sender) {
		                found = true;
		                break;
		            }
		        }
		        require(found, "Permission denied");
		        _;
		    }
		'''
		string='''
		«string1»
		«string2»
		'''
		return string
	}
	
	def compileOperation(Operation operation,Model model){
		var myString = ''''''
		for (everyoperation : operation.operates) {
			var string1 = 'function ' + everyoperation.name + '() internal{'
			var string2 = ''
			if(everyoperation.firstOperation!==null)
				string2=everyoperation.firstOperation.compileTrueOperate(model)
			else if(everyoperation.linkOperation!==null)
				string2=everyoperation.linkOperation.name+'();'
			var string3=''''''
			for (everylink : everyoperation.andOrLink) {
				if(everylink.firstOperation!==null)
					string3+=everylink.firstOperation.compileTrueOperate(model)
				else if(everylink.linkOperation!==null)
					string3+=everylink.linkOperation.name+'();'
			 }
			 myString+='''
			 «string1»
			 	«string2»
			 	«string3»
			 }
			 '''
		}
		 return myString
	}
	
	def compileCondition(Condition condition,Model model) {
	    var myString = ''''''
	    for (everycondition : condition.conditions) {
	        var string1 = 'function ' + everycondition.name + '() internal view returns (bool) {'
	        var string2=''
	        if(everycondition.conditionExpression!==null)
	        	if(everycondition.conditionExpression.no!==null)
	        		string2 ='if (' +'!'+everycondition.conditionExpression.condition.compileTrueCondition(model)
	        	else
	        		string2 ='if (' + everycondition.conditionExpression.condition.compileTrueCondition(model)
	        else if(everycondition.linkCondition!==null)
	        	if(everycondition.linkCondition.no!==null)
	        		string2 ='if (' +'!'+everycondition.linkCondition.linkCondition.name+'()'
	        	else
	        		string2 ='if (' + everycondition.linkCondition.linkCondition.name+'()'
	        for (everylink : everycondition.andOrLink) {
	            if (everylink.link == 'And') {
	            	if(everylink.condition!==null){
		            	if(everylink.condition.no!==null)
		                string2 += '&&' +" !"+everylink.condition.condition.compileTrueCondition(model)
		                else
		                string2 += '&&' +everylink.condition.condition.compileTrueCondition(model)
	                }
	                if(everylink.linkCondition!==null){
		            	if(everylink.linkCondition.no!==null)
		                string2 += '&&' +" !"+everylink.linkCondition.linkCondition.name+'()'
		                else
		                string2 += '&&' +everylink.linkCondition.linkCondition.name+'()'
	                }
	            } else if (everylink.link == 'Or') {
	            	if(everylink.condition.no!==null)
		                string2 += '||' +" !"+everylink.condition.condition.compileTrueCondition(model)
		                else
		                string2 += '||' +everylink.condition.condition.compileTrueCondition(model)
	                }
	                if(everylink.linkCondition!==null){
		            	if(everylink.linkCondition.no!==null)
		                string2 += '||' +" !"+everylink.linkCondition.linkCondition.name+'()'
		                else
		                string2 += '||' +everylink.linkCondition.linkCondition.name+'()'
	            }
	        }
	        string2 += ')'
	        myString +='''
	    «string1»
	    	«string2» return true;
	    	else return false;
	    }
	    '''
	    }
	    return myString
	}

	def dispatch compileTrueCondition(isTime condition,Model model){
		var string=''
		if(condition.expression !== null)
			if(condition.duration===null)
				string='isTime('+condition.expression.compileExpression+')'
			else if(condition.duration!==null)
				string='isTime('+condition.expression.compileExpression+condition.symbol+condition.duration.compilevalue+')'
			if(condition.value !== null)
				if(condition.duration===null)
					string='isTime(functionFinishTime[\"'+condition.value.name+'\"])'
				else if(condition.duration!==null)
					string='isTime(functionFinishTime[\"'+condition.value.name+'\"]+'+condition.symbol+condition.duration.compilevalue+')'
		return string
		}
	
	def dispatch compileTrueCondition(Timepoint timepoint,Model model){
		return timepoint.compileTimePoint2
	}
	
	def dispatch compileTrueCondition(TimeSub condition,Model model){
		var String a=condition.valueA.compileExpression
		var String b=''
		if(condition.valueB!==null)
			b=condition.valueB.compileExpression
		if(condition.valueC!==null)
			b='functionFinishTime[\"'+condition.valueC.name+'\"]'
		var c=condition.duration.compilevalue
		return 'compareTimestamps('+a+','+b+','+c+',\"'+condition.compare+'\")'
	}
	
	def dispatch compileTrueCondition(CompareString condition,Model model){
		return 'compareStrings('+condition.valueA.compileExpression+','+condition.valueB.compileExpression+')'
	}
	
	def dispatch compileTrueCondition(isTrue condition,Model model){
		return condition.compare.compileSingleExpression
	}
	
	def dispatch compileTrueCondition(isDone condition,Model model){
		return 'isDone(\"'+condition.name.name+'\")'
	}
	
	def dispatch compileTrueCondition(logic condition,Model model){
		val tokenMap = new HashMap<String, String>()
	    val tokenObject = model.objects.findFirst[ name == "token" ]
		if (tokenObject !== null) {
	        for (keyValue : tokenObject.objectExpression.keyValue) {
	            // 保存变量名和类型到 tokenMap
	            tokenMap.put(keyValue.name, keyValue.type)
	            println("token - keyvalue.name: " + keyValue.name + ", type: " + keyValue.type)
	        } 
	     }
		 val resultA = addPrefixes(condition.valueA.compileExpression, tokenMap)
		 val resultB = addPrefixes(condition.valueB.compileExpression, tokenMap)
		 
		 
		return 'logic('+resultA+','+resultB+',\"'+condition.mathSymbol+'\")'
	}
	
	def String addPrefixes(String expr, HashMap<String, String> varMap) {
	    // 定义运算符列表，包括更多运算符
	    val operators = #['+', '-', '*', '/', '(', ')']
	
	    // 使用正则表达式分割表达式，同时保留运算符
	    val tokens = expr.split('(?<=[+\\-*/()])|(?=[+\\-*/()])')
	
	    // 处理每个 token，添加相应前缀
	    val prefixedTokens = tokens.map [ token |
	        val trimmed = token.trim
	        val tri = trimmed.startsWith('Service.') ? trimmed.substring(5) : trimmed
	        println("tri: " + tri)
	          if (!operators.contains(trimmed) && trimmed.length > 0 && !trimmed.matches("\\d+")) {
            // 如果 token 在 tokenMap 中，添加前缀 "Service."
		            if (varMap.containsKey(trimmed)) {
		                'Service.' + trimmed
		            } else {
		                trimmed // 如果不在 tokenMap 中，保持不变
		            }
		        } else {
		            trimmed // 运算符或数字保持不变
		        }
		    ]

	
	    // 重新组合表达式，使用空格以增加可读性
	   return prefixedTokens.join('')
	}
	
	
	def compileExpression(MixExpression expression){
		var string=''
		string+=expression.expression.compileSingleExpression
		if(expression.otherMixExpression!==null)
			string+=expression.otherMixExpression.compileOtherMixExpression
		return string
	}
	
	def dispatch compileSingleExpression(OtherExpression expression){
		return expression.value
	}
	
	def dispatch compileSingleExpression(FloatExpression expression){
		return expression.value+'/1000'
	}
	
	def dispatch compileSingleExpression(PeriodExpression expression){
		if(expression.type=="hours")
			return expression.value*60*60
		else if(expression.type=="days")
			return expression.value*60*60*24
		else if(expression.type=="months")
			return expression.value*60*60*24*30
		else if(expression.type=="years")
			return expression.value*60*60*24*30*365
		else if(expression.type=="mins")
			return expression.value*60
	}
	
	def dispatch compileSingleExpression(GetPeriodExpression expression){
		if(expression.type=="hours")
			return expression.value+'*60*60'
		else if(expression.type=="days")
			return expression.value+'*60*60*24'
		else if(expression.type=="months")
			return expression.value+'*60*60*24*30'
		else if(expression.type=="years")
			return expression.value+'*60*60*24*30*365'
		else if(expression.type=="mins")
			return expression.value+'*60'
	}
	
	def dispatch compileSingleExpression(RuleTimeExpression expression){
		return 'functionFinishTime[\"'+expression.value.name+'\"]'
	}
	
	def dispatch compileSingleExpression(PersonExpression expression){
		return '\"'+expression.value.name+'\"'
	}
	
	def dispatch compileSingleExpression(ThingExpression expression){
		return expression.compileThingExpression
	}
	
	def dispatch compileSingleExpression(StringExpression expression){
		return '\"'+expression.value+'\"'
	}
	
	def dispatch compileThingExpression(SubjectExpress expression){
		var string=''
		string=expression.subject.name+'.'+expression.attribute
		return string
	}
	
	def dispatch compileThingExpression(ObjectExpress expression){
		var string=''
		string=expression.object.name+'.'+expression.attribute
		return string	
	}
	
	def dispatch compileSingleExpression(RegularExpression expression){
		return expression.compileRegular
	}
	
	def dispatch compileRegular(AllNumber value){
		return value.number
	}
	
	def dispatch compileRegular(ThisDecimal value){
		return stringToDouble(value.value)*1000
	}
	
	def dispatch compileRegular(ThisBoolean value){
		return value.value
	}
	
	def dispatch compileRegular(Now value){
		return 'block.timestamp'
	}
	
	def dispatch compileRegular(ThisDate value){
		return value.compilevalue
	}
	
	def compileOtherMixExpression(EList<OtherMixExpression> expression){
		var string=''
		for(i : expression)
			string+=i.link+i.expression.compileSingleExpression
		return string
	}

	def dispatch compileTrueCondition(checkExpression condition){
		return "check("+condition.name.name+")"
	}
	
	def compileMessageOperate(messageExpression operation){
		return operation.everymassage.defineEveryMessage
	
	}
	
	def defineEveryMessage(EList<everyMessage> list){
		var isFirst = true
		var result = ""
		for (i : list) {
		    if (!isFirst) {
		        result += ', '
		    }
		    if(i.type=="bool"||i.type=='uint'||i.type=='address'){
		    	result += i.type+" _"+i.message
		    }else if(i.type=="ufixed"){
		    	result += "uint256"+" _"+i.message
		    }
		    else result += i.type+" memory"+" _"+i.message
		    isFirst = false
		}
		return result;
	}
		
	def dispatch compileTrueOperate(transferExpression operation,Model model){
		var string=''
		val tokenMap = new HashMap<String, String>()
	    val tokenObject = model.objects.findFirst[ name == "token" ]
		if (tokenObject !== null) {
	        for (keyValue : tokenObject.objectExpression.keyValue) {
	            // 保存变量名和类型到 tokenMap
	            tokenMap.put(keyValue.name, keyValue.type)
	            println("token - keyvalue.name: " + keyValue.name + ", type: " + keyValue.type)
	        } 
	     }
		
		val a = addPrefixes(operation.value.compileExpression,tokenMap)
		
		string='transferTo('+operation.person.name+'.account,10**14*('+a+'))'+';'
		return string
	}
	
	def dispatch compileTrueOperate(changeExpression operation,Model model){
		return operation.compileChangeExpression(model)
	}
	

	
	def dispatch compileChangeExpression(otherchange expression,Model model){
		var string=''
		val tokenMap = new HashMap<String, String>()
	    val tokenObject = model.objects.findFirst[ name == "token" ]
		if (tokenObject !== null) {
	        for (keyValue : tokenObject.objectExpression.keyValue) {
	            // 保存变量名和类型到 tokenMap
	            tokenMap.put(keyValue.name, keyValue.type)
	            println("token - keyvalue.name: " + keyValue.name + ", type: " + keyValue.type)
	        } 
	     }
		val changething = addPrefixes(expression.changeThing.compileExpression,tokenMap)
		val changeresult = addPrefixes(expression.changeResult.compileExpression,tokenMap)
		string=changething+'='+changeresult+';'
		return string
	}
	
	def dispatch compileChangeExpression(changeString expression,Model model){
		var string=''
		string=expression.changeThing.compileExpression+'=\"'+expression.changeResult.compileExpression+'\";'
		return string
	}
	
	
	
	def dispatch compileChangeExpression(changeContract expression,Model model){
		var string=''
		string='ContractState=\"'+expression.changeResult+'\";'
		return string
	}
	
	def dispatch compileChangeExpression(changeRule expression, Model model){
		var string=''
		string='functionStatus[\"'+expression.changeThing.name+'\"]='+expression.changeResult+';'
		return string
	}
	
	def dispatch compileChangeExpression(changeOther expression,Model model){
		var string=''
		val tokenMap = new HashMap<String, String>()
	    val tokenObject = model.objects.findFirst[ name == "token" ]
		if (tokenObject !== null) {
	        for (keyValue : tokenObject.objectExpression.keyValue) {
	            // 保存变量名和类型到 tokenMap
	            tokenMap.put(keyValue.name, keyValue.type)
	            println("token - keyvalue.name: " + keyValue.name + ", type: " + keyValue.type)
	        } 
	     }
		
		val changeresult = addPrefixes(expression.changeResult,tokenMap)

		string=expression.changeThing.compileSingleExpression+"="+changeresult+';'
		return string
	}
	
	def dispatch compileChangeExpression(changeAddress expression){
		var string=''
		string=expression.changeThing.compileExpression+'='+"payable("+expression.changeResult.compileExpression+');'
		return string
	}
	
	def dispatch compileTrueOperateThing(transferExpression operation)''''''
	
	def compileMessageOperateThing(messageExpression operation) '''
	    «FOR i : operation.everymassage»
	        «IF i.symbol === null»
	            _«i.message» = «i.message»;   // 保持原有赋值
	        «ELSE»
	            «IF i.type == "ufixed"»
	                uint256 «i.message» = uint256(_«i.message»);  // 转换为 uint256
	            «ELSE»
	                «i.type» «i.message» = _«i.message»;   // 使用正确的类型进行赋值
	            «ENDIF»
	        «ENDIF»
	    «ENDFOR»
	'''

	def dispatch compileTrueOperateThing(changeExpression operation)''''''
	
	def compileRule(RuleStructure rule,Model model)'''
	«FOR i : rule.manyRuleExpression»
		«i.compileManyRuleExpression(model)»
	«ENDFOR»
	'''
	
	def dispatch compileManyRuleExpression(ParallelExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileRuleExpression(model)»
		«ENDFOR»
	'''
		
	def dispatch compileManyRuleExpression(ExclusiveExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileExclusiveRuleExpression(model)»
		«ENDFOR»
	'''
	
	def dispatch compileManyRuleExpression(RegularRuleExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileRuleExpression(model)»
		«ENDFOR»
	'''
	
	def dispatch compileManyRuleExpression(AdditionExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileAdditionalRuleExpression(model)»
		«ENDFOR»
	'''
	
	def compileAdditionalRuleExpression(RuleExpression expression, Model model){
		var String code=''''''
		var string1=''
		var link=''
		var setDateString1=''
		var setDateString2=''
		if(expression.setdate!==null){
			setDateString1='uint16 _year,uint8 _months,uint8 _days,uint8 _hours,uint8 _minutes'
			setDateString2=expression.setdate.message+'=convertToTimestamp(_year,_months,_days,_hours,_minutes);'
		}
		if(expression.setdate!==null&&expression.set!==null){
			link=','
		}
		if(expression.repeat===null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+") public payable only"+expression.totalOperation.person.name+" unDone(\""+expression.name+"\"){"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+") public payable only"+expression.totalOperation.person2.name+" unDone(\""+expression.name+"\"){"		
			else
				string1="function "+expression.name+"("+setDateString1+") public payable"+" unDone(\""+expression.name+"\"){"
		}
		if(expression.set!==null)
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable only"+expression.totalOperation.person.name+" unDone(\""+expression.name+"\"){"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable only"+expression.totalOperation.person2.name+" unDone(\""+expression.name+"\"){"
			else
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable"+" unDone(\""+expression.name+"\"){"
		}
		if(expression.repeat!==null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+") public payable only"+expression.totalOperation.person.name+"{"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+") public payable only"+expression.totalOperation.person2.name+"{"		
			else
				string1="function "+expression.name+"("+setDateString1+") public payable {"
		}
		if(expression.set!==null)
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable only"+expression.totalOperation.person.name+"{"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable only"+expression.totalOperation.person2.name+"{"
			else
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable{"
		}	
		var string2=''''''
		if(expression.set!==null)
			string2+=expression.set.compileMessageOperateThing
		var string3=''
		if(expression.totalCondition!==null)
			string3+='if('+expression.totalCondition.compileTotalCondition(model)+'){'
		var string4=''''''
		if(expression.totalOperation!==null)	
		string4+=expression.totalOperation.compileTotalOperation(model)
		
		var string5=''''''
		if(expression.subExpression!==null){
			for(i: expression.subExpression){
				var string6=''
				var string7=''''''
				var string8=''
				var string9=''
				if(i.totalExpression.totalCondition!==null)
					string6+="if("+i.totalExpression.totalCondition.compileTotalCondition(model)+"){"
				string7+=i.totalExpression.totalOperation.compileTotalOperation(model)
				if(i.totalExpression.elseExpression!==null)
					string8='''else{ 
						«i.totalExpression.elseExpression.totalOperation.compileTotalOperation(model)»
						}'''
				if(i.totalExpression.totalCondition!==null)
					string9="}"
				string5+='''
				 	«string6»
				 		«string7»
				 	«string8»
				 		«string9»
				'''
				}
			}
		
//		var string6="functionStatus[\""+expression.name+"\"] = true;"
//		var string63="functionFinishTime[\""+expression.name+"\"]=block.timestamp;"
//		var string62="emit completedRule(msg.sender,\""+expression.name+"\");"
		var string6="changeRule(\""+expression.name+"\");"
		var string7=''''''
		if(expression.totalCondition!==null)
			string7="}"	
		
		code+='''
		«string1»
			«setDateString2»
			«string2»
			«string3»
				«string4»
				«string5»
				«string6»
«««				«string63»
«««				«string62»
			«string7»
		}
		'''
		
		if(expression.elseExpression===null){
			return code
		}
	
		if(expression.elseExpression!==null){
			var string8=''''''
			var string9=''
		if(expression.elseExpression.set===null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"() public payable only"+expression.elseExpression.totalOperation.person.name+"{"
			else if(expression.totalOperation.person2!==null)
				string9="function else"+expression.name+"() public payable only"+expression.elseExpression.totalOperation.person2.name+"{"
			else
				string9="function else"+expression.name+"() public payable {"
		if(expression.elseExpression.set!==null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable only"+expression.elseExpression.totalOperation.person.name+"{"
			else if(expression.elseExpression.totalOperation.person2!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable only"+expression.elseExpression.totalOperation.person2.name+"{"
			else
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable {"

		var onlyOne='''bool one=true;
		if(one){
		'''
		var string10=''
		var string11=''
		var string112="emit completedRule(msg.sender,\"else"+expression.name+"\");"
		var string12=''''''
		var string13=''
		if(expression.totalCondition!==null)
			string10+='if(!('+expression.totalCondition.compileTotalCondition(model)+')){'
		string11+=expression.elseExpression.totalOperation.compileTotalOperation(model)
		if(expression.totalCondition!==null)
			string13="}"
		string8='''
			«string9»
				«onlyOne»
				«string10»
					«string11»
					«string112»
				«string13»
		'''
		code+='''
			«string8»
			one=false;
			}
		'''
		return code
		}
	}
	
	def compileExclusiveRuleExpression(RuleExpression expression,Model model){
		var String code=''''''
		var string1=''
		var link=''
		var setDateString1=''
		var setDateString2=''
		if(expression.setdate!==null){
			setDateString1='uint16 _year,uint8 _months,uint8 _days,uint8 _hours,uint8 _minutes'
			setDateString2=expression.setdate.message+'=convertToTimestamp(_year,_months,_days,_hours,_minutes);'
		}
		if(expression.setdate!==null&&expression.set!==null){
			link=','
		}
		if(expression.repeat===null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+") internal payable onlyState(ContractState) only"+expression.totalOperation.person.name+" unDone(\""+expression.name+"\"){"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+") internal payable onlyState(ContractState) only"+expression.totalOperation.person2.name+" unDone(\""+expression.name+"\"){"		
			else
				string1="function "+expression.name+"("+setDateString1+") internal payable onlyState(ContractState)"+" unDone(\""+expression.name+"\"){"
		}
		if(expression.set!==null)
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") internal payable onlyState(ContractState) only"+expression.totalOperation.person.name+" unDone(\""+expression.name+"\"){"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") internal payable onlyState(ContractState) only"+expression.totalOperation.person2.name+" unDone(\""+expression.name+"\"){"
			else
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") internal payable onlyState(ContractState)"+" unDone(\""+expression.name+"\"){"
		}
		if(expression.repeat!==null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+") internal payable onlyState(ContractState) only"+expression.totalOperation.person.name+"{"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+") internal payable onlyState(ContractState) only"+expression.totalOperation.person2.name+"{"		
			else
				string1="function "+expression.name+"("+setDateString1+") internal payable onlyState(ContractState){"
		}
		if(expression.set!==null)
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") internal payable onlyState(ContractState) only"+expression.totalOperation.person.name+"{"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") internal payable onlyState(ContractState) only"+expression.totalOperation.person2.name+"{"
			else
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") internal payable onlyState(ContractState){"
		}	
		var string2=''''''
		if(expression.set!==null)
			string2+=expression.set.compileMessageOperateThing
		var string3=''
		if(expression.totalCondition!==null)
			string3+='if('+expression.totalCondition.compileTotalCondition(model)+'){'
		var string4=''''''
		if(expression.totalOperation!==null)	
		string4+=expression.totalOperation.compileTotalOperation(model)
		
		var string5=''''''
		if(expression.subExpression!==null){
			for(i: expression.subExpression){
				var string6=''
				var string7=''''''
				var string8=''
				var string9=''
				if(i.totalExpression.totalCondition!==null)
					string6+="if("+i.totalExpression.totalCondition.compileTotalCondition(model)+"){"
				string7+=i.totalExpression.totalOperation.compileTotalOperation(model)
				if(i.totalExpression.elseExpression!==null)
					string8='''else{ 
						«i.totalExpression.elseExpression.totalOperation.compileTotalOperation(model)»
						}'''
				if(i.totalExpression.totalCondition!==null)
					string9="}"
				string5+='''
				 	«string6»
				 		«string7»
				 	«string8»
				 		«string9»
				'''
				}
			}
		
//		var string6="functionStatus[\""+expression.name+"\"] = true;"
//		var string63="functionFinishTime[\""+expression.name+"\"]=block.timestamp;"
//		var string62="emit completedRule(msg.sender,\""+expression.name+"\");"
		var string6="changeRule(\""+expression.name+"\");"
		var string7=''''''
		if(expression.totalCondition!==null)
			string7="}"	
		
		code+='''
		«string1»
			«setDateString2»
			«string2»
			«string3»
				«string4»
				«string5»
				«string6»
«««				«string63»
«««				«string62»
			«string7»
		}
		'''
		
		if(expression.elseExpression===null){
			return code
		}
	
		if(expression.elseExpression!==null){
			var string8=''''''
			var string9=''
		if(expression.elseExpression.set===null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"() public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person.name+"{"
			else if(expression.totalOperation.person2!==null)
				string9="function else"+expression.name+"() public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person2.name+"{"
			else
				string9="function else"+expression.name+"() public payable onlyState(ContractState){"
		if(expression.elseExpression.set!==null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person.name+"{"
			else if(expression.elseExpression.totalOperation.person2!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person2.name+"{"
			else
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) {"

		var onlyOne='''bool one=true;
		if(one){
		'''
		var string10=''
		var string11=''
		var string112="emit completedRule(msg.sender,\"else"+expression.name+"\");"
		var string12=''''''
		var string13=''
		if(expression.totalCondition!==null)
			string10+='if(!('+expression.totalCondition.compileTotalCondition(model)+')){'
		string11+=expression.elseExpression.totalOperation.compileTotalOperation(model)
		if(expression.totalCondition!==null)
			string13="}"
		string8='''
			«string9»
				«onlyOne»
				«string10»
					«string11»
					«string112»
				«string13»
		'''
		code+='''
			«string8»
			one=false;
			}
		'''
		return code
		}
	}
	
	def compileRuleExpression(RuleExpression expression,Model model){
		var String code=''''''
		var string1=''
		var link=''
		var setDateString1=''
		var setDateString2=''
		if(expression.setdate!==null){
			setDateString1='uint16 _year,uint8 _months,uint8 _days,uint8 _hours,uint8 _minutes'
			setDateString2=expression.setdate.message+'=convertToTimestamp(_year,_months,_days,_hours,_minutes);'
		}
		if(expression.setdate!==null&&expression.set!==null){
			link=','
		}
		if(expression.repeat===null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+") public payable onlyState(ContractState) only"+expression.totalOperation.person.name+" unDone(\""+expression.name+"\"){"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+") public payable onlyState(ContractState) only"+expression.totalOperation.person2.name+" unDone(\""+expression.name+"\"){"		
			else
				string1="function "+expression.name+"("+setDateString1+") public payable onlyState(ContractState)"+" unDone(\""+expression.name+"\"){"
		}
		if(expression.set!==null)
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.totalOperation.person.name+" unDone(\""+expression.name+"\"){"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.totalOperation.person2.name+" unDone(\""+expression.name+"\"){"
			else
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable onlyState(ContractState)"+" unDone(\""+expression.name+"\"){"
		}
		if(expression.repeat!==null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+") public payable onlyState(ContractState) only"+expression.totalOperation.person.name+" {"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+") public payable onlyState(ContractState) only"+expression.totalOperation.person2.name+" {"		
			else
				string1="function "+expression.name+"("+setDateString1+") public payable onlyState(ContractState)"+" {"
		}
		if(expression.set!==null)
			if(expression.totalOperation.person!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.totalOperation.person.name+" {"
			else if(expression.totalOperation.person2!==null)
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.totalOperation.person2.name+" {"
			else
				string1="function "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperate+") public payable onlyState(ContractState)"+" {"
		}	
		var string2=''''''
		if(expression.set!==null)
			string2+=expression.set.compileMessageOperateThing
		var string3=''
		if(expression.totalCondition!==null)
			string3=string3+'if('+expression.totalCondition.compileTotalCondition(model)+'){'
		var string4=''''''	
		string4+=expression.totalOperation.compileTotalOperation(model)
		
		var string5=''''''
		if(expression.subExpression!==null){
			for(i: expression.subExpression){
				var string6=''
				var string7=''''''
				var string8=''
				var string9=''
				if(i.totalExpression.totalCondition!==null)
					string6+='if('+i.totalExpression.totalCondition.compileTotalCondition(model)+'){'
				string7+=i.totalExpression.totalOperation.compileTotalOperation(model)
				if(i.totalExpression.elseExpression!==null)
					string8='''else{ 
						«i.totalExpression.elseExpression.totalOperation.compileTotalOperation(model)»
						}'''
				if(i.totalExpression.totalCondition!==null)
					string9="}"
				string5+='''
				 	«string6»
				 		«string7»
				 	«string8»
				 	«string9»
				'''
				}
			}
		
//		var string6="functionStatus[\""+expression.name+"\"] = true;"
//		var string63="functionFinishTime[\""+expression.name+"\"]=block.timestamp;"
//		var string62="emit completedRule(msg.sender,\""+expression.name+"\");"
		var string6="changeRule(\""+expression.name+"\");"
		var string7=''''''
		if(expression.totalCondition!==null)
			string7="}"	
		
		code+='''
		«string1»
			«setDateString2»
			«string2»
			«string3»
				«string4»
				«string5»
				«string6»
«««				«string63»
«««				«string62»
			«string7»
		}
		'''
		
		if(expression.elseExpression===null){
			return code
		}
	
		if(expression.elseExpression!==null){
			var string8=''''''
			var string9=''
		if(expression.elseExpression.set===null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"() public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person.name+" unDone(\""+expression.name+"\"){"
			else if(expression.totalOperation.person2!==null)
				string9="function else"+expression.name+"() public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person2.name+" unDone(\""+expression.name+"\"){"
			else
				string9="function else"+expression.name+"() public payable onlyState(ContractState) unDone(\""+expression.name+"\"){"
		if(expression.elseExpression.set!==null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person.name+" unDone(\""+expression.name+"\"){"
			else if(expression.elseExpression.totalOperation.person2!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person2.name+" unDone(\""+expression.name+"\"){"
			else
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) unDone(\""+expression.name+"\"){"

		var onlyOne='''bool one=true;
		if(one){
		'''
		var string10=''
		var string11=''
		var string112="emit completedRule(msg.sender,\"else"+expression.name+"\");"
		var string12=''''''
		var string13=''
		if(expression.totalCondition!==null)
			string10+='if(!('+expression.totalCondition.compileTotalCondition(model)+')){'
		string11+=expression.elseExpression.totalOperation.compileTotalOperation(model)
		if(expression.totalCondition!==null)
			string13="}"
		if(expression.totalCondition!==null)
			string12="}"
		string8='''
			«string9»
				«onlyOne»
				«string10»
					«string11»
					«string112»
				«string13»
			«string12»
		'''
		code+='''
			«string8»
			one=false;
			}
		'''
		return code
		}
	}
	
	def compileTotalCondition(TotalCondition condition,Model model){
		var String myString = ''
		if(condition.condition!==null)
		if(condition.condition.no!==null)
			myString=myString+'!'+condition.condition.condition.compileTrueCondition(model)
			else
			myString=myString+condition.condition.condition.compileTrueCondition(model)
		if(condition.linkCondition!==null)
			if(condition.linkCondition.no!==null)
				myString=myString+'!'+condition.linkCondition.linkCondition.name+'()'
			else myString=myString+condition.linkCondition.linkCondition.name+'()'
		for(i: condition.andorcondition){
			if(i.link=="and")
				myString=myString+'&&'
			else myString=myString+'||'
			if(i.condition!==null)
			if(i.condition.no!==null)
				myString=myString+'!'+i.condition.condition.compileTrueCondition(model)
			else
				myString=myString+i.condition.condition.compileTrueCondition(model)
			if(i.linkCondition!==null)
				if(i.linkCondition.no!==null)
					myString=myString+'!'+i.linkCondition.linkCondition.name+'()'
				else
					myString=myString+i.linkCondition.linkCondition.name+'()'
		}
		return myString
	}

	def compileTotalOperation(TotalOperation operation,Model model){
		var code=''''''
		var string1=''''''
		string1+='''
		«IF operation.timePoint!==null»
			«operation.timePoint.compileTimePoint»
		«ENDIF»
		'''
		if(operation.failresult!==null){
		string1+='''
			bool completed«con» = false;
			«IF operation.firstOperation!==null»
				«operation.firstOperation.compileTrueOperate(model)»
			«ENDIF»
			«IF operation.linkOperation!==null»
				«operation.linkOperation.compilelinkOperate»
			«ENDIF»
			«FOR i: operation.andor»
				«IF i.firstOperation!==null»
					«i.firstOperation.compileTrueOperate(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperate»
				«ENDIF»
			«ENDFOR»
			completed«con» = true;
			'''
			if(operation.timePoint!==null)
				string1+='}'
			string1+='''
			if(!completed«con»){
				«operation.failresult.compileFailResult(model)»
			}
			'''

			con=con+1
		}
		if(operation.failresult===null){
			string1+='''
			«IF operation.firstOperation!==null»
				«operation.firstOperation.compileTrueOperate(model)»
			«ENDIF»
			«IF operation.linkOperation!==null»
				«operation.linkOperation.compilelinkOperate»
			«ENDIF»
			«FOR i: operation.andor»
				«IF i.firstOperation!==null»
					«i.firstOperation.compileTrueOperate(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperate»
				«ENDIF»
			«ENDFOR»
			'''
			if(operation.timePoint!==null)
				string1+='}'
		}
		var string2=''''''	
		for(i:operation.thenoperation){
			string2+='''
			«IF i.timePoint!==null»
				«i.timePoint.compileTimePoint»
			«ENDIF»'''
			if(i.failresult!==null){
				string2+='''
				bool completed«con» = false;
				«IF i.followingOperation!==null»
					«i.followingOperation.compileTrueOperate(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperate»
				«ENDIF»
				«FOR j: i.andor»
					«IF j.firstOperation!==null»
						«j.firstOperation.compileTrueOperate(model)»
					«ENDIF»
					«IF j.linkOperation!==null»
						«j.linkOperation.compilelinkOperate»
					«ENDIF»
				«ENDFOR»
				completed«con» = true;
				'''
				if(i.timePoint!==null)
					string2+='}'
				string2+='''
				if(!completed«con»){
					«i.failresult.compileFailResult(model)»
				}
				'''
				con=con+1
			}
			
			if(i.failresult===null)
			string2+='''
				«IF i.followingOperation!==null»
					«i.followingOperation.compileTrueOperate(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperate»
				«ENDIF»
				«FOR j: i.andor»
					«IF j.firstOperation!==null»
						«j.firstOperation.compileTrueOperate(model)»
					«ENDIF»
					«IF j.linkOperation!==null»
						«j.linkOperation.compilelinkOperate»
					«ENDIF»
				«ENDFOR»
			'''
			if(i.timePoint!==null)
					string2+='}'
		}
		code+='''
		«string1»
		«string2»
		'''
		return code
	}
	
	def compilelinkOperate(OperateLink link)'''
		«link.name»();
	'''
	
	def compileFailResult(FailResult result, Model model){
		var code=''''''
		var string1=''''''
		string1+='''
		«IF result.timePoint!==null»
			«result.timePoint.compileTimePoint»
		«ENDIF»
		«IF result.followingOperation!==null»
			«result.followingOperation.compileTrueOperate(model)»
		«ENDIF»
		«IF result.linkOperation!==null»
			«result.linkOperation.compilelinkOperate»
		«ENDIF»
		«FOR i: result.andor»
			«IF i.firstOperation!==null»
				«i.firstOperation.compileTrueOperate(model)»
			«ENDIF»
			«IF i.linkOperation!==null»
				«i.linkOperation.compilelinkOperate»
			«ENDIF»
		«ENDFOR»
		'''
		
		var string2=''''''
		for(i:result.thenoperation){
			string2+='''
			«IF i.timePoint!==null»
				«i.timePoint.compileTimePoint»
			«ENDIF»
			'''
			if(i.failresult!==null){
				string2+='''
				bool completed«con» = false;
				«IF i.followingOperation!==null»
					«i.followingOperation.compileTrueOperate(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperate»
				«ENDIF»
				«FOR j: i.andor»
					«IF j.firstOperation!==null»
						«j.firstOperation.compileTrueOperate(model)»
					«ENDIF»
					«IF j.linkOperation!==null»
						«j.linkOperation.compilelinkOperate»
					«ENDIF»
				«ENDFOR»
				completed«con» = true;
				'''
				if(result.timePoint!==null)
					string2+='}'
				string2+='''
				if(!completed«con»){
					«i.failresult.compileFailResult(model)»
				}
				'''
				con=con+1
			}
			string2+='''
			«IF i.failresult===null»
				«IF i.followingOperation!==null»
					«i.followingOperation.compileTrueOperate(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperate»
				«ENDIF»
				«FOR j: i.andor»
					«IF j.firstOperation!==null»
						«j.firstOperation.compileTrueOperate(model)»
					«ENDIF»
					«IF j.linkOperation!==null»
						«j.linkOperation.compilelinkOperate»
					«ENDIF»
				«ENDFOR»
			«ENDIF»
			'''
		}
		code+='''
		«string1»
		«string2»
		'''
		return code
	}
	
	def dispatch compileTimePoint(WithinPoint timepoint){
		var string='''
			«IF timepoint.time=='months'»
				uint256 endTime«time»=functionFinishTime["«timepoint.thing.name»"]+60*60*24*30*«timepoint.number»;
			«ELSEIF timepoint.time=='days'»
				uint256 endTime«time»=functionFinishTime["«timepoint.thing.name»"]+60*60*24*«timepoint.number»;
			«ELSEIF timepoint.time=='hours'»
				uint256 endTime«time»=functionFinsihTime["«timepoint.thing.name»"]+60*60*«timepoint.number»;
			«ENDIF»
			if(!isTime(endTime«time»)){
			'''
			time=time+1
		return string
	}
	
	def dispatch compileTimePoint(BeforePoint timepoint){
		if(timepoint.lineTime!==null)
//		datetime.toTimestamp("+condition.date.value.compilevalue+
			return "if(!isTime(datetime.toTimestamp("+timepoint.lineTime.compilevalue+"))){"
		else if(timepoint.value!==null)
			return "if(!isTime("+timepoint.value.name+")){"
	}
	
	def dispatch compileTimePoint2(WithinPoint timepoint){
		var string='''
			«IF timepoint.time=='months'»
				!isTime(functionFinishTime["«timepoint.thing.name»"]+60*60*24*30*«timepoint.number»)
			«ELSEIF timepoint.time=='days'»
				!isTime(functionFinishTime["«timepoint.thing.name»"]+60*60*24*«timepoint.number»)
			«ELSEIF timepoint.time=='hours'»
				!isTime(functionFinsihTime["«timepoint.thing.name»"]+60*60*«timepoint.number»)
			«ENDIF»
			'''
		return string
	}
	
	def dispatch compileTimePoint2(BeforePoint timepoint){
		if(timepoint.lineTime!==null)
//		datetime.toTimestamp("+condition.date.value.compilevalue+
			return "!isTime(datetime.toTimestamp("+timepoint.lineTime.compilevalue+"))"
		else if(timepoint.value!==null)
			return "!isTime("+timepoint.value.name+")"
	}
	
	//时间转换方法
	def  boolean isLeapYear(int year){
        if (year % 4 != 0) {
            return false;
        }
        if (year % 100 != 0) {
            return true;
        }
        if (year % 400 != 0) {
            return false;
        }
        return true;
    }

	def int getDaysInMonth(int month, int year) {
	    switch month {
	        case 1: return 31
	        case 3: return 31
	        case 5: return 31
	        case 7: return 31
	        case 8: return 31
	        case 10: return 31
	        case 12: return 31
	        case 4: return 30
	        case 6: return 30
	        case 9: return 30
	        case 11: return 30
	        default: return isLeapYear(year) ? 29 : 28
	    }
	}

    def long toTimestamp(int year, int month, int day,int hour,int min){
        if(!(year >= 1970)) println("Year must be 1970 or later");
        if(!(month >= 1 && month <= 12)) println("Invalid month");
		if(!(day >= 1 && day <= getDaysInMonth(month, year))) println("Invalid day");
		if(!(hour >= 0 && hour <= 23)) println("Invalid hour");
		
        var total = 0;
        for (var i = 1970; i < year; i++) {
            total += isLeapYear(i) ? 366 : 365;
        }

        for (var i = 1; i < month; i++) {
            total += getDaysInMonth(i, year);
        }

        total += day - 1;
        val timestamp = total*60*60*24 + hour * 60*60 + min * 60;
        return timestamp;
    }
	


	def compileVyper(Model model)'''
		#pragma version >0.3.10
		
		«IF model.contractMessage !== null»
			« model.contractMessage.compileMessageVyper»
		«ENDIF»
		«FOR subject:model.subjects»
			«subject.compileSubjectVyper»
	    «ENDFOR»
		«FOR object: model.objects»
			«object.compileObjectVyper»
		«ENDFOR»
		«IF model.initialize !== null»
			«model.initialize.compileInitVyper(model)»
		«ENDIF»
		«FOR group : model.group»
			«group.compileGroup»
		«ENDFOR»
«««			constructor() {
«««				«FOR group : model.group»
«««				 	«FOR value :  group.value»
«««				 		«group.name».push(«value.name»);
«««		 	    	«ENDFOR»
«««				«ENDFOR»
«««			}
		event completedRule: 
		    person: indexed(address)
		    ruleName: String[256]
		
		event Transfer: 
		    sender: indexed(address)
		    receiver: indexed(address)
		    value: uint256
		
		event ContractStateChange:
		    newState: String[32]
		«IF model.operations !== null»
			«model.operations.compileOperationVyper(model)»
		«ENDIF»
		«IF model.conditions !== null»
			«model.conditions.compileConditionVyper(model)»
		«ENDIF»
«««			«FOR i : model.ruleStructures.manyRuleExpression»
«««				«i.compileManyRule»
«««			«ENDFOR»
		«IF model.ruleStructures!==null»
			«model.ruleStructures.compileRuleVyper(model)»
		«ENDIF»
		«IF model.require !== null »
			«model.require.compileRequireVyper»
		«ENDIF»

		@internal
		def onlyState(State: String[256]):
		    assert self.logic_str(State, "start","==") or self.logic_str(State, "restart","=="), "Not allowed in this state"

		@internal
		def changeRule(ruleName: String[32]):
		    self.functionStatus[ruleName] = True
		    self.functionFinishTime[ruleName] = block.timestamp
		    log completedRule(msg.sender, ruleName)
		
	'''
	
	
	
	def compileRequireVyper(Require require)'''
		«FOR i :  require.value»
			«IF i == "isTime"»
	    @view
	    def isTime(targetTime: uint256) -> bool:
	        return block.timestamp >= targetTime
			«ELSEIF i == "logic"»
		@view
		def logic(a: uint256, b: uint256, op: String[32]) -> bool:
		    return self.compare_uint(a, b, op)
				
		@view
		def logic_str(a: String[256], b: String[256], op: String[32]) -> bool:
		    return self.compare_string(a, b, op)
		    
		@view
		def compare_uint(a: uint256, b: uint256, op: String[32]) -> bool:
		    if op == ">":
		        return a > b
		    elif op == "<":
		        return a < b
		    elif op == ">=":
		        return a >= b
		    elif op == "<=":
		        return a <= b
		    elif op == "==":
		        return a == b
		    elif op == "!=":
		        return a != b
		    else:
		        raise "Invalid operator."
		
		@view
		def compare_string(a: String[256], b: String[256], op: String[32]) -> bool:
		    if op == "==":
		        return a == b
		    elif op == "!=":
		        return a != b
		    else:
		        raise "Invalid operator."
	
			«ELSEIF i == "isDone"»
	    @internal
	    @view
	    def isDone(functionName: String[32]) -> bool:
	        return self.functionStatus[functionName]
			
			«ELSEIF i == "isCompleted"»
		function isCompleted(string memory ruleName) internal view returns (bool){
			return true;
		}
			«ELSEIF i == "SetDate"»
	    function isLeapYear(uint16 year) internal pure returns (bool) {
	        if (year % 4 != 0) {
	            return false;
	        }
	        if (year % 100 != 0) {
	            return true;
	        }
	        if (year % 400 != 0) {
	            return false;
	        }
	        return true;
	    }
	
	    function getDaysInMonth(uint8 month, uint16 year) internal pure returns (uint8) {
	        if (month == 1 || month == 3 || month == 5 || month == 7 || month == 8 || month == 10 || month == 12) {
	            return 31;
	        } else if (month == 4 || month == 6 || month == 9 || month == 11) {
	            return 30;
	        } else if (isLeapYear(year)) {
	            return 29;
	        } else {
	            return 28;
	        }
	    }
	
	    function convertToTimestamp(uint16 year, uint8 month, uint8 day, uint8 hour, uint8 minute) public pure returns (uint256) {
	        require(year >= 1970, "Year must be 1970 or later");
	        require(month >= 1 && month <= 12, "Invalid month");
	        require(day >= 1 && day <= getDaysInMonth(month, year), "Invalid day");
	        require(hour >= 0 && hour <= 23, "Invalid hour");
	        require(minute >= 0 && minute <= 59, "Invalid minute");
	        uint256 timestamp = 0;
	        for (uint16 y = 1970; y < year; y++) {
	            if (isLeapYear(y)) {
	                timestamp += 366 days;
	            } else {
	                timestamp += 365 days;
	            }
	        }
	        for (uint8 m = 1; m < month; m++) {
	            timestamp += uint256(getDaysInMonth(m, year)) * 1 days;
	        }
	        timestamp += uint256(day - 1) * 1 days;
	        timestamp += uint256(hour) * 1 hours;
	        timestamp += uint256(minute) * 1 minutes;
	        return timestamp;
	    }
			«ELSEIF i == "timeSub"»
	     def compareTimestamps(timestamp1: uint256, timestamp2: uint256, custom_seconds: uint256, operator: String[32]) -> bool:
	        
	         time_difference: uint256 = timestamp1 - timestamp2
	        
	         if operator == ">":
	             return time_difference > custom_seconds
	         elif operator =="<":
	             return time_difference < custom_seconds
	         elif operator == "==":
	             return time_difference == custom_seconds
	         elif operator == "!=":
	             return time_difference != custom_seconds
	         else:
	             raise "Invalid operator."
			«ELSEIF i == "transfer"»
		
		@internal
		@payable
		def transferTo(recipient: address, amount: uint256):
		    assert recipient != 0x0000000000000000000000000000000000000000, "Invalid recipient address"
		    assert amount > 0, "Amount must be greater than zero"
		    send(recipient, amount)
		    log Transfer(msg.sender, recipient, amount)
		    «ELSEIF i == "compareString"»
		    
		def compareStrings(str1: String[256], str2: String[256]) -> bool:
	        return str1 == str2
		    			
			«ENDIF»
			
	    «ENDFOR»
	'''
	
	def compileMessageVyper(ContractMessage message)'''
		«FOR i :  message.message»
			«IF i.type.type == "date"|| i.type.type=="uint"»
			«i.type.name» : public (uint256)
			«ELSEIF i.type.type == "address"»
    	    «i.type.name» : payable(«i.value.compilevalue»)
			«ELSEIF i.type.type == "duration"»
			«i.type.name» : public (uint256)
			«ELSEIF i.type.type == "ufixed" || i.type.type == "fixed" »
			«i.type.name» : public (uint256)
			«ELSEIF i.type.type == "string"»
			«i.type.name» : public (String[256])
			«ELSEIF i.type.type == "bool"»
			«i.type.name» : public (bool)
			«ENDIF»
	    «ENDFOR»
		ContractState: public(String[256])
		functionStatus: public(HashMap[String[32],bool])
		functionFinishTime: public(HashMap[String[32],uint256])	
	'''
	def compileSubjectVyper(Subject subject) '''
«««	建立结构体
struct «subject.name»: 
	«IF subject.type != "CA"»
	name: String[256]
	account: address
		«ENDIF»
		«IF subject.type == "CA"»
	name: String[256]
	account: address
	key: uint256
	year: uint256
		«ENDIF»
		«IF subject.subjectExpression !== null»
			«subject.subjectExpression.compileSubjectExpressionVyper»
		«ENDIF»
	
	'''
	
	def compileSubjectExpressionVyper(SubjectExpression SuE){'''
		«FOR key :  SuE.keyValue»
			«IF key.type == "date" || key.type == "ufixed"»
				«key.name» : uint256 ;
				«ELSEIF key.type == "string"»
    				«key.name» : String[256]
				«ELSEIF key.type != "date"&&key.type != "ufixed" && key.type!="string"»
    				   «key.name» : «key.type»
    				   
    	    «ENDIF»
    	«ENDFOR»
    '''
	}
	
	def compileObjectVyper(Object object)'''
	«««	建立结构体
struct «object.name»: 
	«object.objectExpression.compileObjectExpressionVyper»


'''
	def compileObjectExpressionVyper(ObjectExpression ObE) {'''
		«FOR key :  ObE.keyValue»
			«IF key.type == "date" || key.type == "ufixed" || key.type == "uint"»
					«key.name»: uint256
				«ELSEIF key.type == "string"»
					«key.name»: String[256] 
				«ELSEIF key.type != "date"&&key.type != "ufixed" &&key.type != "string"»
    				«key.name»: «key.type» 
    		«ENDIF»
    	«ENDFOR»
    '''
	}
		
	def compileInitVyper(Initialize init,Model model){'''
	«««初始化主体
		«FOR sinit:init.inits»
			«sinit.name»: public(«sinit.subtype.name»)
		«ENDFOR»
	
	«««初始化客体
		«FOR oinit:init.inito»
			«oinit.name»: public(«oinit.obtype.name»)
		«ENDFOR»
		
	@deploy
	def __init__():
		«IF model.contractMessage !== null»
			«FOR i :  model.contractMessage.message»
				self.«i.type.name» = «i.value.compilevalueVyper»
		    «ENDFOR»
		«ENDIF»
		self.ContractState = "start"
		«FOR sinit:init.inits»
			«IF sinit.subtype.name == "Person"» 
			self.«sinit.name» = «sinit.subtype.name» (name = «sinit.value.compilevaluelistVyper("Person",model)»)
			«ENDIF»
			«IF sinit.subtype.name == "Ca"» 
			self.«sinit.name» = «sinit.subtype.name» (name = «sinit.value.compilevaluelistVyper("Ca",model)»)
			«ENDIF»
		«ENDFOR»
		
		«FOR oinit:init.inito»
			self.«oinit.name» = «oinit.obtype.name»(«oinit.value.compilevaluelistVyper("token",model)»)
		«ENDFOR»
	
	«««建立相应的modifier
		«FOR sinit:init.inits»
			@internal
			def only«sinit.name»():
				assert msg.sender == self.«sinit.name».account, "Only «sinit.name» can access this."
			
		«ENDFOR»
	'''	
	}
	def compilevaluelistVyper(EList<Value> list, String type ,Model model){
		var isFirst = true
		var result = ""
		if (type == "Person") {
			for (value : list) {
			    if (!isFirst) {
			        result += ', account='
			    }
			    result += value.compilevalueVyper
			    isFirst = false
			}
			return result
		} else if (type == "Ca"){
			var i = 0
			for (value : list) {
			    if (i==1) {
			        result += ', account='
			    }
			    if (i==2) {
			        result += ', key='
			    }
			    if (i==3) {
			        result += ', year='
			    }
			    result += value.compilevalueVyper
			    i++
			}
			return result
		} else if (type=="token"){
			var index = 0  // 用于跟踪索引
	        // 遍历 model.objects 获取属性名称
	        for (object : model.objects) {
	            for (key : object.objectExpression.keyValue) {
	                // 使用 key.name 动态生成属性
	                if (!isFirst) {
	                    result += ', '
	                }
	                // 通过 index 获取 list 中的相应值
	                result += key.name + '=' + list.get(index).compilevalueVyper
	                isFirst = false
	                index++  // 增加索引
	            }
	        }
	        return result
		}	
	}
	
	
	def dispatch compilevalueVyper(Address value){
		return value.value
	}
	
	def dispatch compilevalueVyper(AllNumber value){
		return value.number
	}
	
	def dispatch compilevalueVyper(Now value){
		return 'block.timestamp'
	}
	
	def dispatch compilevalueVyper(ThisDecimal value){
		return  Math.round(stringToDouble(value.value) * 1000) as int
	}
	
	def dispatch compilevalueVyper(url value){
		return value.value
	}
	
	def dispatch compilevalueVyper(ThisString value){
		return '"'+value.value+'"'
	}
	
	def dispatch compilevalueVyper(ThisBoolean value){
		return value.value.toFirstUpper
	}
	
	def dispatch compilevalueVyper(Duration value){
		if(value.symbol=="months")
			return (value.value)*60*60*24*30
		else if(value.symbol=="days")
			return (value.value)*60*60*24
		else if(value.symbol=="mins")
			return (value.value)*60
		else if(value.symbol=="hours")
			return (value.value)*60*60
		else if(value.symbol=="years")
			return (value.value)*60*60*24*30*365
	}
	
	def dispatch compilevalueVyper(ThisDate value){
		return toTimestamp(value.value.year,value.value.month,value.value.day,value.value.hour,value.value.min)
	}
	
	def dispatch compilevalueVyper(Right value){
		return '"'+value.right.name+'"'
	}

	def compileOperationVyper(Operation operation,Model model){
		var myString = ''''''
		for (everyoperation : operation.operates) {
			var string1 = 'def ' + everyoperation.name + '():'
			var string2 = ''
			if(everyoperation.firstOperation!==null)
				string2=everyoperation.firstOperation.compileTrueOperateVyper(model)
			else if(everyoperation.linkOperation!==null)
				string2="self."+everyoperation.linkOperation.name+'()'
			var string3=''''''
			for (everylink : everyoperation.andOrLink) {
				if(everylink.firstOperation!==null)
					string3+=everylink.firstOperation.compileTrueOperateVyper(model)
				else if(everylink.linkOperation!==null)
					string3+="self."+everylink.linkOperation.name+'()'
			 }
			 myString+='''
			 «string1»
			 	«string2»
			 	«string3»
			 
			 '''
		}
		 return myString
	}
	
	
	def dispatch compileTrueOperateVyper(transferExpression operation,Model model){
		var string=''
		string='self.transferTo('+'self.'+operation.person.name+'.account,10**14*('+operation.value.compileExpressionVyper(model)+'))'
		return string
	}
	
	def dispatch compileTrueOperateVyper(changeExpression operation,Model model){
		return operation.compileChangeExpressionVyper(model)
	}
	

	
	def dispatch compileChangeExpressionVyper(otherchange expression,Model model){
		var string=''
		string=expression.changeThing.compileExpressionVyper(model)+'='+expression.changeResult.compileExpressionVyper(model)
		return string
	}
	
	def dispatch compileChangeExpressionVyper(changeString expression,Model model){
		var string=''
		string=expression.changeThing.compileExpressionVyper(model)+'=\"'+expression.changeResult.compileExpressionVyper(model)+'\"'
		return string
	}
	
	
	
	def dispatch compileChangeExpressionVyper(changeContract expression,Model model){
		var string=''
		string='self.ContractState=\"'+expression.changeResult+'\"'
		return string
	}
	
	def dispatch compileChangeExpressionVyper(changeRule expression,Model model){
		var string=''
		string='self.functionStatus[\"'+expression.changeThing.name+'\"]='+expression.changeResult.toFirstUpper
		return string
	}
	
	def dispatch compileChangeExpressionVyper(changeOther expression,Model model){
		var string=''
		string=expression.changeThing.compileSingleExpressionVyper(model)+"="+expression.changeResult
		return string
	}
	
	def dispatch compileChangeExpressionVyper(changeAddress expression,Model model){
		var string=''
		string="self."+expression.changeThing.compileExpressionVyper(model)+'='+expression.changeResult.compileExpression
		return string
	}
	
	
	def compileConditionVyper(Condition condition,Model model) {
	    var myString = ''''''
	    for (everycondition : condition.conditions) {
	    	var string1 = '@internal \n'
	        string1 += 'def ' + everycondition.name + '() -> bool:'
	        var string2=''
	        if(everycondition.conditionExpression!==null)
	        	if(everycondition.conditionExpression.no!==null)
	        		string2 ='if ' +'not'+everycondition.conditionExpression.condition.compileTrueConditionVyper(model)
	        	else
	        		string2 ='if ' +everycondition.conditionExpression.condition.compileTrueConditionVyper(model)
	        else if(everycondition.linkCondition!==null)
	        	if(everycondition.linkCondition.no!==null)
	        		string2 ='if ' +'not '+ 'self.'+everycondition.linkCondition.linkCondition.name+'()'
	        	else
	        		string2 ='if '+ 'self.'+everycondition.linkCondition.linkCondition.name+'()'
	        for (everylink : everycondition.andOrLink) {
	            if (everylink.link == 'And') {
	            	if(everylink.condition!==null){
		            	if(everylink.condition.no!==null)
		                string2 += ' and ' +"not "+everylink.condition.condition.compileTrueConditionVyper(model)
		                else
		                string2 += ' and ' +everylink.condition.condition.compileTrueConditionVyper(model)
	                }
	                if(everylink.linkCondition!==null){
		            	if(everylink.linkCondition.no!==null)
		                string2 += ' and ' +"not "+ 'self.'+everylink.linkCondition.linkCondition.name+'()'
		                else
		                string2 += ' and ' +'self.' +everylink.linkCondition.linkCondition.name+'()'
	                }
	            } else if (everylink.link == 'Or') {
	            	if(everylink.condition.no!==null)
		                string2 += ' or' +" not"+everylink.condition.condition.compileTrueConditionVyper(model)
		                else
		                string2 += ' or'+everylink.condition.condition.compileTrueConditionVyper(model)
	                }
	                if(everylink.linkCondition!==null){
		            	if(everylink.linkCondition.no!==null)
		                string2 += ' or' +" not "+'self.'+everylink.linkCondition.linkCondition.name+'()'
		                else
		                string2 += ' or' +' self.'+everylink.linkCondition.linkCondition.name+'()'
	            }
	        }
	    	string2 += ':'
	        myString +='''
	    «string1»
	    	«string2» return True
	    	else:
	    	 	return False
	    
	    '''
	    }
	    return myString
	}
	
	def dispatch compileTrueConditionVyper(isTime condition,Model model){
		var string=''
		if(condition.expression !== null)
			if(condition.duration===null)
				string='self.isTime('+condition.expression.compileExpressionVyper(model)+')'
			else if(condition.duration!==null)
				string='self.isTime('+condition.expression.compileExpressionVyper(model)+'self.'+condition.symbol+condition.duration.compilevalue+')'
			if(condition.value !== null)
				if(condition.duration===null)
					string='self.isTime(self.functionFinishTime[\"'+condition.value.name+'\"])'
				else if(condition.duration!==null)
					string='self.isTime(self.functionFinishTime[\"'+condition.value.name+'\"]+'+condition.symbol+condition.duration.compilevalue+')'
		return string
		}
	
	def dispatch compileTrueConditionVyper(Timepoint timepoint,Model model){
		return timepoint.compileTimePointVyper
	}

	def dispatch compileTrueConditionVyper(TimeSub condition,Model model){
		var String a=condition.valueA.compileExpressionVyper(model)
		var String b=''
		if(condition.valueB!==null)
			b=condition.valueB.compileExpressionVyper(model)
		if(condition.valueC!==null)
			b='self.functionFinishTime[\"'+condition.valueC.name+'\"]'
		var c=condition.duration.compilevalueVyper
		return 'self.compareTimestamps('+a+','+b+','+c+',\"'+condition.compare+'\")'
	}
	
	def dispatch compileTrueConditionVyper(CompareString condition,Model model){
		return 'self.compareStrings('+condition.valueA.compileExpressionVyper(model)+','+condition.valueB.compileExpressionVyper(model)+')'
	}
	
	def dispatch compileTrueConditionVyper(isTrue condition,Model model){
		return condition.compare.compileSingleExpressionVyper(model)
	}
	
	def dispatch compileTrueConditionVyper(isDone condition,Model model){
		return 'self.isDone(\"'+condition.name.name+'\")'
	}
	
	def dispatch compileTrueConditionVyper(logic condition, Model model) {

		val tokenMap = new HashMap<String, String>()
	    val contractMessageMap = new HashMap<String, String>()
	    
	    // 查找名为 "token" 的对象，并将其属性和类型添加到 tokenMap
	    val tokenObject = model.objects.findFirst[ name == "token" ]
	    if (tokenObject !== null) {
	        for (keyValue : tokenObject.objectExpression.keyValue) {
	            // 保存变量名和类型到 tokenMap
	            tokenMap.put(keyValue.name, keyValue.type)
	            println("token - keyvalue.name: " + keyValue.name + ", type: " + keyValue.type)
	        }
	    }
	    
	    val contractMessage = model.contractMessage
	    if (contractMessage !== null) {
	        for (messageKey : contractMessage.message) {
	            // 保存消息变量名和类型到 contractMessageMap
	            contractMessageMap.put(messageKey.type.name, messageKey.type.type)
	            println("contractMessage - messageKey.type.name: " + messageKey.type.name + ", type: " + messageKey.type.type)
	        }
	    }
	    
	    // 去掉 'self.' 前缀并编译表达式
	    val valueA = condition.valueA.compileExpressionVyper(model)
	    val valueB = condition.valueB.compileExpressionVyper(model)
	    println("valueA:" + valueA + " valueB:" + valueB)
	    
	    // 传递去掉前缀后的值到 addPrefixes，获取处理后的表达式（带有 true 或 false 标志）
	    val resultA = addPrefixes(valueA, tokenMap, contractMessageMap)
	    val resultB = addPrefixes(valueB, tokenMap, contractMessageMap)
	    
	    // 从结果中提取 true/false 标志和处理后的表达式
	    val isStringA = resultA.startsWith('true')
	    val isStringB = resultB.startsWith('true')
	    
	    // 从结果中去掉 "true " 或 "false " 前缀，获取实际的前缀化表达式
	    val prefixedValueA = resultA.substring(5)
	    val prefixedValueB = resultB.substring(5)
	    
	    // 根据第一个变量的类型判断调用哪个函数
	    if (isStringA || isStringB) {
	        return 'self.logic_str(' + prefixedValueA + ', ' + prefixedValueB + ', "' + condition.mathSymbol + '")'
	    } else {
	        return 'self.logic(' + prefixedValueA + ', ' + prefixedValueB + ', "' + condition.mathSymbol + '")'
	    }
	}
	
def String addPrefixes(String expr, HashMap<String, String> tokenMap, HashMap<String, String> contractMessageMap) {
    // 定义运算符列表，包括更多运算符
    val operators = #['+', '-', '*', '/', '(', ')']
    
    // 使用正则表达式分割表达式，同时保留运算符
    val tokens = expr.split('(?<=[+\\-*/()])|(?=[+\\-*/()])')
    
    // 处理每个 token，添加相应前缀
    val prefixedTokens = tokens.map [ token |
        val trimmed = token.trim
        var tri = trimmed
        
        // 如果 token 是 'block.timestamp'，不加前缀
        if (tri == 'block.timestamp') {
            return tri
        }
        
        // 如果 token 不在 tokenMap 和 contractMessageMap 中，直接返回
        if (!tokenMap.containsKey(tri) && !contractMessageMap.containsKey(tri)) {
            return tri
        }

        tri = tri.startsWith('self.') ? tri.substring(5) : tri
        println("tri: " + tri)
        
        // 进行变量名匹配并添加前缀
        if (!operators.contains(tri) && tri.length > 0 && !tri.matches("\\d+")) {
            var prefix = 'self.'  // 默认前缀
            
            // 如果 token 在 tokenMap 中，使用对应的前缀 self.Service
            if (tokenMap.containsKey(tri)) {
                prefix = 'self.Service.'
            }
            // 如果 token 在 contractMessageMap 中，使用对应的前缀 self
            else if (contractMessageMap.containsKey(tri)) {
                prefix = 'self.'
            }
            
            return prefix + tri
        } else {
            return tri
        }
    ]
    
    // 获取第一个变量的类型来判断是否为字符串
    val firstToken = tokens.head.trim
    
    // 检查第一个 token 是否有前缀 'self.'，并进行类型判断
    if (firstToken.startsWith('self.')) {
        val tokenName = firstToken.substring(5) // 去掉 'self.'
        
        // 使用 if-else 来替代 ?: 操作符
        val isString = if (tokenMap.get(tokenName) != null && tokenMap.get(tokenName).equals('string')) {
            true
        } else if (contractMessageMap.get(tokenName) != null && contractMessageMap.get(tokenName).equals('string')) {
            true
        } else {
            false
        }
        
        // 返回 true 或 false + 拼接后的表达式
        if (isString) {
            return 'true ' + prefixedTokens.join('') // 返回 true + 拼接表达式
        } else {
            return 'false ' + prefixedTokens.join('') // 返回 false + 拼接表达式
        }
    } else {
        return 'false ' + prefixedTokens.join('') // 默认返回 false + 拼接表达式
    }
}





	

	
	
	def compileExpressionVyper(MixExpression expression,Model model){
		var string=''
		string+=expression.expression.compileSingleExpressionVyper(model)
		if(expression.otherMixExpression!==null)
			string+=expression.otherMixExpression.compileOtherMixExpressionVyper(model)
		return string
	}
	
	def dispatch compileSingleExpressionVyper(OtherExpression expression,Model model){
		val tokenMap = new HashMap<String, String>()
	    val contractMessageMap = new HashMap<String, String>()
	    
	    // 查找名为 "token" 的对象，并将其属性和类型添加到 tokenMap
	    val tokenObject = model.objects.findFirst[ name == "token" ]
	    if (tokenObject !== null) {
	        for (keyValue : tokenObject.objectExpression.keyValue) {
	            // 保存变量名和类型到 tokenMap
	            tokenMap.put(keyValue.name, keyValue.type)
	            println("token - keyvalue.name: " + keyValue.name + ", type: " + keyValue.type)
	        }
	    }
	    
	    val contractMessage = model.contractMessage
	    if (contractMessage !== null) {
	        for (messageKey : contractMessage.message) {
	            // 保存消息变量名和类型到 contractMessageMap
	            contractMessageMap.put(messageKey.type.name, messageKey.type.type)
	            println("contractMessage - messageKey.type.name: " + messageKey.type.name + ", type: " + messageKey.type.type)
	        }
	    }
	
		    // 去掉 'self.' 前缀
	    var value = expression.value
//	    if (value.startsWith("service.")) {
//	        value= value.substring(8)
//		}
	    // 移除 'self.' 前缀
//	    val cleanedValueA = valueA.startsWith('self.') ? valueA.substring(5) : valueA
//	    val cleanedValueB = valueB.startsWith('self.') ? valueB.substring(5) : valueB
	
	    // 传递去掉前缀后的值到 addPrefixes
	    val prefixedValue = addPrefixes(value, tokenMap,contractMessageMap)
	    val prefixedValueA = prefixedValue.substring(5)
	
	    // 生成最终的 Vyper 代码字符串
	  
		return prefixedValueA
	}
	
	def dispatch compileSingleExpressionVyper(FloatExpression expression,Model model){
		return 'self.'+expression.value+'//1000'
	}
	
	def dispatch compileSingleExpressionVyper(RegularExpression expression,Model model){
		return expression.compileRegular
	}
	def dispatch compileSingleExpressionVyper(PeriodExpression expression,Model model){
		if(expression.type=="hours")
			return expression.value*60*60
		else if(expression.type=="days")
			return expression.value*60*60*24
		else if(expression.type=="months")
			return expression.value*60*60*24*30
		else if(expression.type=="years")
			return expression.value*60*60*24*30*365
		else if(expression.type=="mins")
			return expression.value*60
	}
	
	def dispatch compileSingleExpressionVyper(GetPeriodExpression expression,Model model){
		if(expression.type=="hours")
			return 'self.'+expression.value+'*60*60'
		else if(expression.type=="days")
			return 'self.'+expression.value+'*60*60*24'
		else if(expression.type=="months")
			return 'self.'+expression.value+'*60*60*24*30'
		else if(expression.type=="years")
			return 'self.'+expression.value+'*60*60*24*30*365'
		else if(expression.type=="mins")
			return 'self.'+expression.value+'*60'
	}
	
	def dispatch compileSingleExpressionVyper(RuleTimeExpression expression,Model model){
		return 'self.functionFinishTime[\"'+expression.value.name+'\"]'
	}
	
	def dispatch compileSingleExpressionVyper(PersonExpression expression,Model model){
		return '\"'+expression.value.name+'\"'
	}
	
	def dispatch compileSingleExpressionVyper(ThingExpression expression,Model model){
		return expression.compileThingExpressionVyper
	}
	
	def dispatch compileThingExpressionVyper(ObjectExpress expression){
		var string=''
		string='self.'+expression.object.name+'.'+expression.attribute
		return string	
	}
	def dispatch compileThingExpressionVyper(SubjectExpress expression){
		var string=''
		string='self.'+expression.subject.name+'.'+expression.attribute
		return string
	}
	
	
	def dispatch compileSingleExpressionVyper(StringExpression expression,Model model){
		return '\"'+expression.value+'\"'
	}
	
	def compileOtherMixExpressionVyper(EList<OtherMixExpression> expression,Model model){
		var string=''
		for(i : expression)
			string+=i.link+i.expression.compileSingleExpressionVyper(model)
		return string
	}
	
	def dispatch compileTimePointVyper(WithinPoint timepoint){
		var string='''
			«IF timepoint.time=='months'»
				not self.isTime(self.functionFinishTime["«timepoint.thing.name»"]+60*60*24*30*«timepoint.number»)
			«ELSEIF timepoint.time=='days'»
				not self.isTime(self.functionFinishTime["«timepoint.thing.name»"]+60*60*24*«timepoint.number»)
			«ELSEIF timepoint.time=='hours'»
				not self.isTime(self.functionFinsihTime["«timepoint.thing.name»"]+60*60*«timepoint.number»)
			«ENDIF»
			'''
		return string
	}
	
	def dispatch compileTimePointVyper(BeforePoint timepoint){
		if(timepoint.lineTime!==null)
//		datetime.toTimestamp("+condition.date.value.compilevalue+
			return "not self.isTime(datetime.toTimestamp("+timepoint.lineTime.compilevalue+"))"
		else if(timepoint.value!==null)
			return "not self.isTime(self."+timepoint.value.name+")"
	}
	
	def compileRuleVyper(RuleStructure rule,Model model)'''
	«FOR i : rule.manyRuleExpression»
		«i.compileManyRuleExpressionVyper(model)»
	«ENDFOR»
	'''
	
	def dispatch compileManyRuleExpressionVyper(ParallelExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileRuleExpressionVyper(model)»
		«ENDFOR»
	'''
		
	def dispatch compileManyRuleExpressionVyper(ExclusiveExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileExclusiveRuleExpressionVyper(model)»
		«ENDFOR»
	'''
	
	def dispatch compileManyRuleExpressionVyper(RegularRuleExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileRuleExpressionVyper(model)»
		«ENDFOR»
	'''
	
	def dispatch compileManyRuleExpressionVyper(AdditionExpression expression,Model model)'''
		«FOR i : expression.ruleExpression»
			«i.compileAdditionalRuleExpressionVyper(model)»
		«ENDFOR»
	'''
	def compileRuleExpressionVyper(RuleExpression expression,Model model){
		var String code=''''''
		var string1=''
		var link=''
		var setDateString1=''
		var setDateString2=''
		if(expression.setdate!==null){
			setDateString1='_year: uint32,_months: uint8,_days: uint8,_hours: uint8,_minutes: uint8 '
			setDateString2=expression.setdate.message+'=convertToTimestamp(_year,_months,_days,_hours,_minutes);'
		}
		if(expression.setdate!==null&&expression.set!==null){
			link=','
		}
		if(expression.repeat===null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"+"	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"+"	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""
				}		
			else{
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1+="	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
		}
		if(expression.set!==null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"+"	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"+"	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			else{
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n "
				string1 += "	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			}
		}
		if(expression.repeat!==null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n" 
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"+"	self.onlyState(self.ContractState)\n"}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"+"	self.onlyState(self.ContractState)\n"}		
			else{
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.onlyState(self.ContractState)\n"}
		}
		if(expression.set!==null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"+"	self.onlyState(self.ContractState)\n"
				}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"): \n"
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"+"	self.onlyState(self.ContractState)\n"}
			
			else{
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"): \n"
				string1 += "	self.onlyState(self.ContractState)\n"}
				}
		}
			
		var string2=''''''
		if(expression.set!==null)
			string2+=expression.set.compileMessageOperateThingVyper
		var string3=''
		if(expression.totalCondition!==null)
			string3=string3+'if '+expression.totalCondition.compileTotalConditionVyper(model)+':'
		var string4=''''''	
		string4+=expression.totalOperation.compileTotalOperationVyper(model)
		
		var string5=''''''
		if(expression.subExpression!==null){
			for(i: expression.subExpression){
				var string6=''
				var string7=''''''
				var string8=''
				var string9=''
				if(i.totalExpression.totalCondition!==null)
					string6+='if '+i.totalExpression.totalCondition.compileTotalConditionVyper(model)+':'
				string7+=i.totalExpression.totalOperation.compileTotalOperationVyper(model)
				if(i.totalExpression.elseExpression!==null)
					string8='''else: 
						«i.totalExpression.elseExpression.totalOperation.compileTotalOperationVyper(model)»
						'''
				if(i.totalExpression.totalCondition!==null)
					string9=""
				string5+='''
				 	«string6»
				 		«string7»
				 	«string8»
				 	«string9»
				'''
				}
			}
		
//		var string6="functionStatus[\""+expression.name+"\"] = true;"
//		var string63="functionFinishTime[\""+expression.name+"\"]=block.timestamp;"
//		var string62="emit completedRule(msg.sender,\""+expression.name+"\");"
		var string6="self.changeRule(\""+expression.name+"\")"
		var string7=''''''
		if(expression.totalCondition!==null)
			string7=""	
		
		code+='''
		«string1»
			«setDateString2»
			«string2»
			«string3»
				«string4»
				«string5»
				«string6»
«««				«string63»
«««				«string62»
			«string7»
		
		'''
		
		if(expression.elseExpression===null){
			return code
		}
	
		if(expression.elseExpression!==null){
			var string8=''''''
			var string9=''
		if(expression.elseExpression.set===null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"() public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person.name+" unDone(\""+expression.name+"\"){"
			else if(expression.totalOperation.person2!==null)
				string9="function else"+expression.name+"() public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person2.name+" unDone(\""+expression.name+"\"){"
			else
				string9="function else"+expression.name+"() public payable onlyState(ContractState) unDone(\""+expression.name+"\"){"
		if(expression.elseExpression.set!==null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person.name+" unDone(\""+expression.name+"\"){"
			else if(expression.elseExpression.totalOperation.person2!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person2.name+" unDone(\""+expression.name+"\"){"
			else
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) unDone(\""+expression.name+"\"){"

		var onlyOne='''bool one=true
		if(one)
		'''
		var string10=''
		var string11=''
		var string112="emit completedRule(msg.sender,\"else"+expression.name+"\")"
		var string12=''''''
		var string13=''
		if(expression.totalCondition!==null)
			string10+='if not('+expression.totalCondition.compileTotalConditionVyper(model)+')'
		string11+=expression.elseExpression.totalOperation.compileTotalOperationVyper(model)
		if(expression.totalCondition!==null)
			string13=""
		if(expression.totalCondition!==null)
			string12=""
		string8='''
			«string9»
				«onlyOne»
				«string10»
					«string11»
					«string112»
				«string13»
			«string12»
		'''
		code+='''
			«string8»
			one=false
			
		'''
		return code
		}
	}
	
	def compileExclusiveRuleExpressionVyper(RuleExpression expression,Model model){
		var String code=''''''
		var string1=''
		var link=''
		var setDateString1=''
		var setDateString2=''
		if(expression.setdate!==null){
			setDateString1='_year: uint16,_months: uint8,_days: uint8,_hours: uint8,_minutes: uint8'
			setDateString2=expression.setdate.message+'=convertToTimestamp(_year,_months,_days,_hours,_minutes);'
		}
		if(expression.setdate!==null&&expression.set!==null){
			link=','
		}
		if(expression.repeat===null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"+"	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"+"	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""
				}		
			else{
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""
				}
		}
		if(expression.set!==null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"+"	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"+"	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			else{
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.onlyState(self.ContractState)\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
		  }
		}
		if(expression.repeat!==null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"+"	self.onlyState(self.ContractState)\n"}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"	
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"+"	self.onlyState(self.ContractState)\n"}
			else{
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.onlyState(self.ContractState)\n"}
		}
		if(expression.set!==null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"+"	self.onlyState(self.ContractState)\n"}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"+"	self.onlyState(self.ContractState)\n"}
			else{
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.onlyState(self.ContractState)\n"
				}
			}
		}	
		var string2=''''''
		if(expression.set!==null)
			string2+=expression.set.compileMessageOperateThingVyper
		var string3=''
		if(expression.totalCondition!==null)
			string3+='if '+expression.totalCondition.compileTotalConditionVyper(model)+':'
		var string4=''''''
		if(expression.totalOperation!==null)	
		string4+=expression.totalOperation.compileTotalOperationVyper(model)
		
		var string5=''''''
		if(expression.subExpression!==null){
			for(i: expression.subExpression){
				var string6=''
				var string7=''''''
				var string8=''
				var string9=''
				if(i.totalExpression.totalCondition!==null)
					string6+="if "+i.totalExpression.totalCondition.compileTotalConditionVyper(model)+":"
				string7+=i.totalExpression.totalOperation.compileTotalOperationVyper(model)
				if(i.totalExpression.elseExpression!==null)
					string8='''else: 
						«i.totalExpression.elseExpression.totalOperation.compileTotalOperationVyper(model)»
						'''
				if(i.totalExpression.totalCondition!==null)
					string9=""
				string5+='''
				 	«string6»
				 		«string7»
				 	«string8»
				 		«string9»
				'''
				}
			}
		
//		var string6="functionStatus[\""+expression.name+"\"] = true;"
//		var string63="functionFinishTime[\""+expression.name+"\"]=block.timestamp;"
//		var string62="emit completedRule(msg.sender,\""+expression.name+"\");"
		var string6="self.changeRule(\""+expression.name+"\")"
		var string7=''''''
		if(expression.totalCondition!==null)
			string7=""	
		
		code+='''
		«string1»
			«setDateString2»
			«string2»
			«string3»
				«string4»
				«string5»
				«string6»
«««				«string63»
«««				«string62»
			«string7»
		
		'''
		
		if(expression.elseExpression===null){
			return code
		}
	
		if(expression.elseExpression!==null){
			var string8=''''''
			var string9=''
		if(expression.elseExpression.set===null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"() public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person.name+"{"
			else if(expression.totalOperation.person2!==null)
				string9="function else"+expression.name+"() public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person2.name+"{"
			else
				string9="function else"+expression.name+"() public payable onlyState(ContractState){"
		if(expression.elseExpression.set!==null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person.name+"{"
			else if(expression.elseExpression.totalOperation.person2!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) only"+expression.elseExpression.totalOperation.person2.name+"{"
			else
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable onlyState(ContractState) {"

		var onlyOne='''bool one=true
		if(one)
		'''
		var string10=''
		var string11=''
		var string112="emit completedRule(msg.sender,\"else"+expression.name+"\")"
		var string12=''''''
		var string13=''
		if(expression.totalCondition!==null)
			string10+='if not('+expression.totalCondition.compileTotalConditionVyper(model)+')'
		string11+=expression.elseExpression.totalOperation.compileTotalOperationVyper(model)
		if(expression.totalCondition!==null)
			string13=""
		string8='''
			«string9»
				«onlyOne»
				«string10»
					«string11»
					«string112»
				«string13»
		'''
		code+='''
			«string8»
			one=false;
			
		'''
		return code
		}
	}
	
	def compileAdditionalRuleExpressionVyper(RuleExpression expression,Model model){
		var String code=''''''
		var string1=''
		var link=''
		var setDateString1=''
		var setDateString2=''
		if(expression.setdate!==null){
			setDateString1='_year: uint16,_months: uint8,_days: uint8,_hours: uint8,_minutes: uint8'
			setDateString2=expression.setdate.message+'=convertToTimestamp(_year,_months,_days,_hours,_minutes);'
		}
		if(expression.setdate!==null&&expression.set!==null){
			link=','
		}
		if(expression.repeat===null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}		
			else{
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
				
		}
		if(expression.set!==null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"+"	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			else{
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	assert not self.isDone(\""+expression.name+"\"),\""+expression.name+" already done\""}
			}
		}
		if(expression.repeat!==null){
		if( expression.set===null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"	
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"}	
			else
				string1="@external\ndef "+expression.name+"("+setDateString1+"):\n"
				
		}
		if(expression.set!==null){
			if(expression.totalOperation.person!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
				string1 += "	self.only"+expression.totalOperation.person.name+"()\n"}
			else if(expression.totalOperation.person2!==null){
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"): \n"
				string1 += "	self.only"+expression.totalOperation.person2.name+"()\n"}
			else
				string1="@external\ndef "+expression.name+"("+setDateString1+link+expression.set.compileMessageOperateVyper+"):\n"
			}
		}	
		var string2=''''''
		if(expression.set!==null)
			string2+=expression.set.compileMessageOperateThingVyper
		var string3=''
		if(expression.totalCondition!==null)
			string3+='if '+expression.totalCondition.compileTotalConditionVyper(model)+':'
		var string4=''''''
		if(expression.totalOperation!==null)	
		string4+=expression.totalOperation.compileTotalOperationVyper(model)
		
		var string5=''''''
		if(expression.subExpression!==null){
			for(i: expression.subExpression){
				var string6=''
				var string7=''''''
				var string8=''
				var string9=''
				if(i.totalExpression.totalCondition!==null)
					string6+="if "+i.totalExpression.totalCondition.compileTotalConditionVyper(model)+":"
				string7+=i.totalExpression.totalOperation.compileTotalOperationVyper(model)
				if(i.totalExpression.elseExpression!==null)
					string8='''else: 
						«i.totalExpression.elseExpression.totalOperation.compileTotalOperationVyper(model)»
						'''
				if(i.totalExpression.totalCondition!==null)
					string9=""
				string5+='''
				 	«string6»
				 		«string7»
				 	«string8»
				 		«string9»
				'''
				}
			}
		
//		var string6="functionStatus[\""+expression.name+"\"] = true;"
//		var string63="functionFinishTime[\""+expression.name+"\"]=block.timestamp;"
//		var string62="emit completedRule(msg.sender,\""+expression.name+"\");"
		var string6="self.changeRule(\""+expression.name+"\")"
		var string7=''''''
		if(expression.totalCondition!==null)
			string7=""	
		
		code+='''
		«string1»
			«setDateString2»
			«string2»
			«string3»
				«string4»
				«string5»
				«string6»
«««				«string63»
«««				«string62»
			«string7»
		
		'''
		
		if(expression.elseExpression===null){
			return code
		}
	
		if(expression.elseExpression!==null){
			var string8=''''''
			var string9=''
		if(expression.elseExpression.set===null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"() public payable only"+expression.elseExpression.totalOperation.person.name+"{"
			else if(expression.totalOperation.person2!==null)
				string9="function else"+expression.name+"() public payable only"+expression.elseExpression.totalOperation.person2.name+"{"
			else
				string9="function else"+expression.name+"() public payable {"
		if(expression.elseExpression.set!==null)
			if(expression.elseExpression.totalOperation.person!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable only"+expression.elseExpression.totalOperation.person.name+"{"
			else if(expression.elseExpression.totalOperation.person2!==null)
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable only"+expression.elseExpression.totalOperation.person2.name+"{"
			else
				string9="function else"+expression.name+"("+expression.elseExpression.set.compileMessageOperate+") public payable {"

		var onlyOne='''bool one=true
		if(one)
		'''
		var string10=''
		var string11=''
		var string112="emit completedRule(msg.sender,\"else"+expression.name+"\")"
		var string12=''''''
		var string13=''
		if(expression.totalCondition!==null)
			string10+='if not ('+expression.totalCondition.compileTotalConditionVyper(model)+')'
		string11+=expression.elseExpression.totalOperation.compileTotalOperationVyper(model)
		if(expression.totalCondition!==null)
			string13=""
		string8='''
			«string9»
				«onlyOne»
				«string10»
					«string11»
					«string112»
				«string13»
		'''
		code+='''
			«string8»
			one=false
			
		'''
		return code
		}
	}
	
	def compileMessageOperateVyper(messageExpression operation){
		return operation.everymassage.defineEveryMessageVyper

	}
	
	def defineEveryMessageVyper(EList<everyMessage> list){
		var isFirst = true
		var result = ""
		for (i : list) {
		    if (!isFirst) {
		        result += ', '
		    }
		    if(i.type=="bool"||i.type=='address')
		    	result += "_"+i.message+": "+i.type
		    else if(i.type=='uint'||i.type=='ufixed')
		    	result += "_"+i.message+": "+"uint256"
		    else if (i.type=='string'){
		    	result += "_"+i.message+": "+"String[256]"
		    }
		    isFirst = false
		}
		return result;
	}
	
	def compileMessageOperateThingVyper(messageExpression operation)'''
		«FOR i : operation.everymassage»
		 «IF i.symbol ===null»
			self.«i.message» = _«i.message»
		«ELSE»
			«IF i.type=="bool" ||i.type=='address'»
				«i.message»: «i.type» = _«i.message»
			«ELSEIF i.type=='uint'||i.type=='ufixed'» 
				«i.message»: uint256 = _«i.message»
			«ELSEIF i.type=='string'» 
				«i.message»: String[256] = _«i.message»

			«ENDIF»
		«ENDIF»
		«ENDFOR»   
	'''

	def compileTotalConditionVyper(TotalCondition condition,Model model){
		var String myString = ''
		if(condition.condition!==null)
		if(condition.condition.no!==null)
			myString=myString+'not '+condition.condition.condition.compileTrueConditionVyper(model)
			else
			myString=myString+condition.condition.condition.compileTrueConditionVyper(model)
		if(condition.linkCondition!==null)
			if(condition.linkCondition.no!==null)
				myString=myString+'not '+'self.'+condition.linkCondition.linkCondition.name+'()'
			else myString=myString+'self.'+condition.linkCondition.linkCondition.name+'()'
		for(i: condition.andorcondition){
			if(i.link=="and")
				myString=myString+' and '
			else myString=myString+' or '
			if(i.condition!==null)
			if(i.condition.no!==null)
				myString=myString+'not '+i.condition.condition.compileTrueConditionVyper(model)
			else
				myString=myString+i.condition.condition.compileTrueConditionVyper(model)
			if(i.linkCondition!==null)
				if(i.linkCondition.no!==null)
					myString=myString+'not '+'self.'+i.linkCondition.linkCondition.name+'()'
				else
					myString=myString+'self.'+i.linkCondition.linkCondition.name+'()'
		}
		return myString
	}

	def compileTotalOperationVyper(TotalOperation operation,Model model){
		var code=''''''
		var string1=''''''
		string1+='''
		«IF operation.timePoint!==null»
			«operation.timePoint.compileTimePointVyper»
		«ENDIF»
		'''
		if(operation.failresult!==null){
		string1+='''
			bool completed«con» = false
			«IF operation.firstOperation!==null»
				«operation.firstOperation.compileTrueOperateVyper(model)»
			«ENDIF»
			«IF operation.linkOperation!==null»
				«operation.linkOperation.compilelinkOperateVyper»
			«ENDIF»
			«FOR i: operation.andor»
				«IF i.firstOperation!==null»
					«i.firstOperation.compileTrueOperateVyper(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperateVyper»
				«ENDIF»
			«ENDFOR»
			completed«con» = true
			'''
			if(operation.timePoint!==null)
				string1+=''
			string1+='''
			if not completed«con»:
				«operation.failresult.compileFailResult(model)»
			
			'''

			con=con+1
		}
		if(operation.failresult===null){
			string1+='''
			«IF operation.firstOperation!==null»
				«operation.firstOperation.compileTrueOperateVyper(model)»
			«ENDIF»
			«IF operation.linkOperation!==null»
				«operation.linkOperation.compilelinkOperateVyper»
			«ENDIF»
			«FOR i: operation.andor»
				«IF i.firstOperation!==null»
					«i.firstOperation.compileTrueOperateVyper(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperateVyper»
				«ENDIF»
			«ENDFOR»
			'''
			if(operation.timePoint!==null)
				string1+=''
		}
		var string2=''''''	
		for(i:operation.thenoperation){
			string2+='''
			«IF i.timePoint!==null»
				«i.timePoint.compileTimePointVyper»
			«ENDIF»'''
			if(i.failresult!==null){
				string2+='''
				bool completed«con» = false
				«IF i.followingOperation!==null»
					«i.followingOperation.compileTrueOperateVyper(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperateVyper»
				«ENDIF»
				«FOR j: i.andor»
					«IF j.firstOperation!==null»
						«j.firstOperation.compileTrueOperateVyper(model)»
					«ENDIF»
					«IF j.linkOperation!==null»
						«j.linkOperation.compilelinkOperateVyper»
					«ENDIF»
				«ENDFOR»
				completed«con» = true
				'''
				if(i.timePoint!==null)
					string2+='}'
				string2+='''
				if not completed«con»:
					«i.failresult.compileFailResult(model)»
				
				'''
				con=con+1
			}
			
			if(i.failresult===null)
			string2+='''
				«IF i.followingOperation!==null»
					«i.followingOperation.compileTrueOperateVyper(model)»
				«ENDIF»
				«IF i.linkOperation!==null»
					«i.linkOperation.compilelinkOperateVyper»
				«ENDIF»
				«FOR j: i.andor»
					«IF j.firstOperation!==null»
						«j.firstOperation.compileTrueOperateVyper(model)»
					«ENDIF»
					«IF j.linkOperation!==null»
						«j.linkOperation.compilelinkOperateVyper»
					«ENDIF»
				«ENDFOR»
			'''
			if(i.timePoint!==null)
					string2+=''
		}
		code+='''
		«string1»
		«string2»
		'''
		return code
	}
	
	def compilelinkOperateVyper(OperateLink link)'''
		self.«link.name»()
	'''
}








